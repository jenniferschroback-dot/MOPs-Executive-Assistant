# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-07-08] DECISION: Built initial skill scaffolds for intake-classification, campaign-naming, sf-campaign-spec, and intake-tracking, with unconfirmed schema/convention details explicitly flagged rather than guessed. | REASONING: No campaign naming convention or Salesforce record type/status schema was confirmed yet; guessing silently would repeat the exact "naming drift" and "wrong status scaffolding" problems these skills are meant to fix. | CONTEXT: First skills built after initial MOPS assistant setup; refine each skill's placeholders as real Asana/Salesforce schema and a naming convention get confirmed through use.

[2026-07-08] DECISION: Campaign naming convention is `type_subtype_region_description_year_quarter` (e.g. `evt_ws_all_dam workshop boston_2026_q3`). | REASONING: Forkan confirmed this is the actual format to use, replacing the draft/no-convention state. | CONTEXT: Only `evt` (type), `ws` (subtype), and `all` (region) are confirmed codes so far — see `.claude/skills/campaign-naming/SKILL.md` for the growing code table.

[2026-07-10] DECISION: Campaign naming convention is superseded — now `Region_Channel_Product_Description_YYYY-Qn`, replacing the 2026-07-08 `type_subtype_region_description_year_quarter` format. | REASONING: Forkan confirmed this as the correct/current format while reviewing the MOps-weekly-report skill. | CONTEXT: Only `Region=All` carries over as confirmed; Channel and Product codes are unconfirmed under the new segment structure — ask rather than invent, per `.claude/skills/campaign-naming/SKILL.md`.

[2026-07-11] DECISION: Which intake requests become Salesforce Campaigns is determined by the Asana `[MOPs] Intake` project's `Project Type` custom field (gid `1206591746930193`) — confirmed campaign-producing values are `Webinar Request`, `Event (+ SFDC Campaign)`, `SFDC Campaign only (single)`, `SFDC Campaigns only (multiple)`, and `UTM(s) (+ SFDC Campaign)`; all other values (`Email(s) only`, `Audiences`, `Reporting`, `List Upload`, `Form Request`, `Other`, internal-ops types) do not. | REASONING: Cross-referenced ~100 recent completed Asana tasks against live Salesforce Campaign records by name — e.g. "Conductor - Partner Master Class" (Webinar Request) resolved to two real Campaigns (Webinar-type + Email-type), while "SCSM Customer Innovation Session Email Report" (Reporting) had no corresponding Campaign creation. | CONTEXT: Encoded into `.claude/skills/intake-classification/SKILL.md` (classification table) and `.claude/skills/sf-campaign-spec/SKILL.md` (gate before proposing a campaign, step 2 of Process).

[2026-07-13] DECISION: `List Upload` and `Form Request` are not unconditional "no Campaign" cases — before skipping, check whether a Campaign already exists for the named event/webinar. `List Upload` needs a new Campaign the ~30% of the time the event/webinar is partner-hosted with no existing Acquia Campaign; `Form Request` needs one the ~30% of the time the web team built the landing page ahead of any Campaign creation. | REASONING: Harish walked through both scenarios live in the 2026-07-13 meeting (`transcripts/Harish_Forkane meeting .txt`) — his manual process already makes this distinction, the 2026-07-11 table was too coarse. | CONTEXT: Updated `.claude/skills/intake-classification/SKILL.md` (table) and `.claude/skills/sf-campaign-spec/SKILL.md` (step 2 exception).

[2026-07-13] DECISION: Webinar/Event requests only get a companion Email-type Campaign when the stakeholder answers yes to a promotional-email flag ("do you require a promotional email as part of this request?") — not automatically. When created, the Event/Webinar Campaign is set as the Email Campaign's Parent Campaign in Salesforce, and both share the same name except the `Channel` segment. | REASONING: Harish demoed this exact manual pattern (parent campaign linkage) and clarified that not every webinar/event needs the email leg — it depends on whether promotional email was requested. | CONTEXT: `intake-classification/SKILL.md` step 4 (flag capture), `sf-campaign-spec/SKILL.md` steps 3-4 (trigger + parent linkage), `campaign-naming/SKILL.md` step 6 (naming pattern).

[2026-07-13] DECISION: Email channel naming gets a `Nurture` subtype, separate from the general `Email` code that covers all other one-off email types (promotional, post-webinar, post-event, etc). Exact short code not locked — Harish suggested `NURT` but said "or anything you find suitable." | REASONING: Harish distinguishes nurture-sequence requests from one-off email sends in his naming; wanted this reflected before the convention is treated as fully closed. | CONTEXT: `campaign-naming/SKILL.md` known-codes table — confirm the exact code with Harish before treating it as final. Also flagged: Salesforce's Campaign Name field has a hard character limit that stakeholder-provided description text can exceed (Harish's manual workaround was silent truncation); exact cap not finalized (40-50 chars suggested) — see same skill file, step 5.

[2026-07-13] DECISION: Multi-send email requests (e.g. 3-4 promotional emails for one webinar) get scheduled as individual dated Asana milestone sub-tasks — one per send, `Project Type` = `Email`, converted to a Milestone — forming a shared email send calendar used to catch same-day/same-audience clashes and to report sent/upcoming emails. | REASONING: This is Harish's existing manual process, demoed live; formalizing it as a skill avoids re-deriving the pattern each time and lets `MOps-weekly-report` query it consistently. | CONTEXT: New skill `.claude/skills/email-send-calendar/SKILL.md`; referenced from `intake-classification/SKILL.md` (step 5-6) and `MOps-weekly-report/SKILL.md` (new slides 5 and 12, added to the retro/look-ahead sections).

[2026-07-13] DECISION: Milestone creation mechanics are confirmed — pass `resource_subtype: "milestone"` directly on Asana task creation (`create_tasks`), and pass `custom_fields` as a JSON string (not a nested object) when setting fields like `Project Type` in the same call. | REASONING: Verified live against the `[MOPs] Intake` project by running the full pipeline (classification → naming → email-send-calendar) end-to-end on the "email campaign for a new AI web building assistant (demo)" task — the milestone sub-task was created successfully on the first attempt with these exact parameters. | CONTEXT: Removes the "unconfirmed mechanics" caveat previously in `email-send-calendar/SKILL.md` step 5. Same run also confirmed the `UTM(s) (+ SFDC Campaign)` `Project Type` option no longer exists in the live Asana enum (only the 15 values now in `intake-classification/SKILL.md`'s table are real) — worth reconciling that table next time it's touched.

[2026-07-20] DECISION: Building a manager-facing "MOps Command Center" — an agentic OS for Jennifer Schroback (Sr. Manager, Agentic Marketing Ops) — as a composition layer over existing skills, NOT a new codebase. Architecture: (1) a read-only `mops-command-center` skill that computes one `briefing` object from live Asana/Salesforce/Jira; (2) a live web dashboard (Artifact) as the single pane of glass; (3) a proactive routine (weekday morning brief + deduped daytime alerts to Slack). Chosen surface = live web dashboard; authority = read + Asana/Slack actions with confirmation; cadence = daily brief + alerts. | REASONING: The reporting skills (`intake-tracking`, `parbot-email-performance`, `MOps-weekly-report`) already pull this data but on separate cadences with no single surface and no proactive push between Thursday reviews; the manager wanted one always-current view. Hard constraint: Salesforce MCP is read/query-only, so the OS reports on campaigns/email but cannot create/edit SF records — writable actions limited to Asana/Slack/Jira. | CONTEXT: This session authored `.claude/skills/mops-command-center/SKILL.md` + `dashboard-template.html` (snapshot render target) and `routines/mops-command-center/README.md` (blueprint, not yet provisioned). Live-dashboard (`mcp` self-refresh) and Slack sends require an authorized interactive session — the `mcp` capability page can't be publicly shared and needs a real request/response observed per connector tool before shipping. Open items: Slack delivery target (DM vs. channel), whether the dashboard also serves Aayushi (lead), and whether the cloud routine env can publish an Artifact.

[2026-07-21] DECISION: Shipped the self-refreshing, Acquia-branded MOPS Command Center dashboard (`.claude/skills/mops-command-center/dashboard-live.html`), published to the same Artifact URL as the earlier snapshot. It declares the `mcp` runtime capability and calls the viewer's own Salesforce (`soqlQuery`) + Asana (`get_tasks`) connectors on each visit, paginating and computing the briefing in-browser (logic keyed off custom-field GIDs — Project Type 1206591746930193, Priority 1211656326879675, MOPS-Status 1210850678809573 — to avoid the trailing-space fragility on the "Priority " field name). | REASONING: The primary Salesforce/Asana connectors turned out to be authenticated in-session, so real data + real tool-response shapes were observable — the prerequisite for building live `mcp` calls. Compute logic was unit-tested in Node against the real page-1 task payload before publishing (open rate 9.60%, CTR 0.31%, −4.55pt delta reproduced exactly; workload/overdue/unclassified/null-priority all scaled correctly). Branding applied from the real Acquia system (navy #232C61 header, Sky Blue #26A3DD droplet motif + inlined wordmark, Acquia Blue #036BB5 titles, status colors Pink/Orange/Sky). | CONTEXT: Tradeoffs — an `mcp`-declaring page is PRIVATE per-viewer and cannot be shared by public link; each viewer (e.g. Jennifer) must have Salesforce + Asana connectors enabled in their own claude.ai. On-time % is deliberately shown as "—" (the 'Out of SLA/Rush' flag is unpopulated; needs a due-date-vs-completion measure). Per-section error handling branches on the mcp McpError codes (needs_reauth/server_not_connected/etc.). The browser `window.claude.mcp` runtime itself could NOT be tested from this CLI session — only the pure compute was verified — so the first real open should be sanity-checked. Snapshot form (`dashboard-template.html`) retained for the routine + shareable use.

[2026-07-21] DECISION: Added a "Pipeline-influenced $ on email" metric — created skill `.claude/skills/pipeline-influenced-email/SKILL.md` (single source of truth) and wired a pipeline section into the live command-center dashboard's email card. Metric = SUM(AmountAllOpportunities) + SUM(NumberOfOpportunities) + SUM(NumberOfWonOpportunities count) from Salesforce Campaign rollups, over email campaigns (TotalEmailsDelivered>0, Type!='Operational'); derived ratios $/1k-delivered and response→opp rate. Dashboard shows: Pipeline influenced $, opps, won count, $/1k sent, response→opp %, and a top-5-emails-by-pipeline table; updates with the quarter selector. | REASONING: Forkan asked whether email has a direct monetary result; live SFDC query confirmed AmountAllOpportunities is populated and meaningful (e.g. $99K on the top email), while won $ (AmountWonOpportunities) is unreliable (0s, a −$2,502) and ExpectedRevenue/ActualCost are null org-wide. So pipeline is reportable but won$/ROI are not. | CONTEXT: Attribution is single-touch (Primary Campaign Source) — undercounts email's assist role; true multi-touch would need the CampaignInfluence object (not yet checked/enabled). Pipeline lags the send, so recent windows look lower — compare like-for-like periods only; no period-over-period pipeline delta shown to avoid a false "decline." Amounts may be currency-converted — confirm single reporting currency before cross-region sums. All computed client-side in the dashboard from the same row-level SOQL used for engagement; verified in Node against realistic figures ($187K/126 opps/$4,785 per 1k). Footnote on the card states the attribution + data-gap caveats.

[2026-07-21] DECISION: Replaced the placeholder on-time tile ("—", based on the near-empty "Out of SLA/Rush" flag) with a REAL on-time SLA %: business-day turnaround (Asana created_at → completed_at) vs a per-Project-Type turnaround SLA, over the last 30 days of completions. Live validation returned ~33% on-time, avg 17.5 business days (Email 23% / SFDC single 11% / List Upload 80%) — consistent with the documented ~47%-ish reality, not the false 97% the flag gave. Implemented in the command-center dashboard (computeOnTime + bizDaysBetween) and defined in `mops-command-center/SKILL.md` Step 3.A. | REASONING: The "Out of SLA/Rush" flag is populated on ~1/35 completed tickets, so it can't measure on-time. created_at is the best submission proxy available. CRITICAL modeling call: the documented "Webinar 2 weeks out / Event 4 weeks out" are REQUESTOR lead-time-before-event rules, NOT MOps turnaround SLAs — scoring created→completed against them produced spurious 0%/28-day results, so Webinar/Event (and no-SLA types: Reporting, Audiences, Automations|Martech, Form Request, Other, UAT, Issues, 6Sense CE, Team OOO, unset) are EXCLUDED from the on-time %, counted separately. | CONTEXT: Turnaround SLA map (business days): SFDC single 1, SFDC multiple 2, UTM(+SFDC) 2, Email/Nurture 5, List Upload 7 (upper of 3-7), IT/Integration 15 (upper of 7-15). Business days ignore holidays; created_at = submission proxy. Dashboard now fetches incomplete backlog + 30-day completions in one paginated get_tasks (completed_since=30d ago) and splits them (avoids the search_tasks 100-cap). On-time-below-target alert fires under 80% (warn under 50%). Footnote on the card states the method + exclusions. Open follow-on: model event-date-based lead-time SLA for Webinar/Event separately if wanted.

[2026-07-21] DECISION: Aligned the on-time SLA model to the official "MOps Requests: SLA Timeline Requirements (2025)" doc (now saved at references/sops/mops-sla-timeline.md). Changes: ADDED Form Request = 5 bd and Reporting (Salesforce Reports) = 5 bd to the turnaround SLA map (both were previously excluded) — on-time recomputed to ~34% over 61 scored completions (Reporting 42%, Email 23%, SFDC single 11%, List Upload 80%). CONFIRMED the Webinar (2 wks out) / Event (4 wks out) exclusion: the doc explicitly anchors those to the live event date and counts the MOps deliverable chain from the 1st promotional send date — so a created→completed turnaround is the wrong measure; they stay excluded. | REASONING: Forkan surfaced the authoritative SLA doc; it validated the turnaround-vs-lead-time split and corrected two mappings I'd guessed as "no SLA." | CONTEXT: Documented approximations the Asana Project Type enum forces (in mops-command-center/SKILL.md Step 3.A): SFDC "multiple" → 2 bd though the doc's real breakpoint is >5 campaigns (enum lacks count); single-email 5 bd is also applied to multi-send requests whose true SLA is 8 bd (enum can't distinguish); UTMs-only (1 bd) has no enum value, only UTM(+SFDC)=2 bd. Still-excluded: Audiences, Automations|Martech, Other, UAT, Issues, 6Sense CE, Team OOO, Routing (7-15 bd but no enum value), unset. Other doc facts captured in the SOP for future use: multi-email 8 bd chain, deliverables checklist, 3-emails/week/audience cap, and the weekday send-ownership calendar (Mon webinars / Tue nurture / Wed ABM / Thu events / Fri rest) — a future dashboard enhancement could flag day-type mismatches and >3/week/audience directly.

[2026-07-21] DECISION: Shipped the first action-layer feature on the live command-center dashboard — an "Unassigned tickets" card with a per-row owner picker → inline confirm → Asana update_tasks write (assigns an owner). This turns the dashboard from read-only into a tool the team acts from, targeting the biggest lever (60 unassigned). | REASONING: Forkan approved building the Assign action first. Verified the write path by observing a REAL update_tasks request/response in-session (a no-op self-assign on my own "Task 1", assignee="me", zero change) — response shape is {data:[{gid,name}], failed:[], summary}, so success = failed[] empty. Looked up the team's Asana user GIDs (Harish 1203937989075417, Aayushi 1212534096012004, Felipe 1214552680652024, Jennifer 1214047208203035, Forkane 1215719015567131) for the picker. | CONTEXT: Added "update_tasks" to the page's mcp manifest (Asana tools now get_tasks + update_tasks). Writes run with the VIEWER's credentials (properly attributed). Guardrails: inline confirm before every write (no native confirm — sandbox-safe), additive-only (assign, no deletes), and "maybe it ran" handling for server_unavailable/upstream_error/cancelled (no auto-retry; a ↺ reset lets the user re-pick as a fresh gesture). Salesforce stays read-only. Could NOT exercise the in-browser click path from the CLI — only the API-shape write was verified — so the first real assignment should be sanity-checked on a test ticket. Next action-layer candidates: Set Priority (136 null), Nudge owner (Slack DM), both following this same confirm→callTool→toast pattern.

[2026-07-26] DECISION: Converted the command-center dashboard's magnitude-by-category metrics from thin horizontal progress-fill "bars" (which read as plain lines) to proper vertical bar charts. Applies to all four such metrics: Capacity Workload (open tasks by person), Ticket status by stage, Ticket aging buckets, and Triage-lag distribution — in both `dashboard-live.html` and (for Workload, the only one it renders) `dashboard-template.html`. | REASONING: Forkan asked for bar charts instead of "just a plain line" on Capacity Workload "and the similar metrics." Followed the `dataviz` skill: form = bars for magnitude-by-category; single hue (sky→blue) with a warning accent (orange→pink) for flagged categories (Unassigned / Waiting-for-Feedback+Incomplete / 1–3mo+3mo+ / >3 business days) — no categorical palette, so no ΔE validator run needed; baseline-anchored 4px-rounded tops, direct value labels above each bar, per-bar hover (title + brightness), theme-aware via existing CSS vars. | CONTEXT: Added a reusable `barChart(items,{empty})` helper + `.vbars` CSS to both files (128px plot, 20px headroom so the tallest bar's value label isn't clipped, zero values keep a 3px baseline stub, >5 bars → dense gap) and a `shortName()` for compact person labels (full name kept in the hover title). Removed the now-unused `.bar/.track/.fill` CSS. Logic verified in Node (height scaling, name-shortening, empty/all-zero cases, HTML escaping); the in-browser render itself wasn't screenshot-tested from the CLI. KPI stat tiles (email open/CTR/sends, pipeline) were left as tiles — they're single headline numbers, not category series.

[2026-07-26] DECISION: Added an "Email Performance Report" generator to the mops-command-center live dashboard (`dashboard-live.html`) — on-demand full email-performance brief for any quarter/month/week, computed client-side from one live Salesforce Campaign pull, with a Copy-Markdown export in the exact written-brief format. | REASONING: The manager/marketing team needed reproducible, reliable email reports (same structure/format as the hand-written Q2 2026 brief and the parbot deck) with a selectable timeline, not just the fixed trailing-30/quarter tile the dashboard already had. Computing on-screen and export from the same row set guarantees they reconcile; using the Campaign rollup `AmountAllOpportunities` keeps the number equal to the `pipeline-influenced-email` single-source metric the brief used. | CONTEXT: Verified in node against the live Q2 2026 pull — reconciled to 78 campaigns / 1,089,367 delivered / 95,472 opens / 5,555 clicks / $7,386,728 pipeline; cohorts, top lists, red flags matched the standalone Python analysis. Live surface only (needs the viewer's Salesforce connector); read-only. Documented in `.claude/skills/mops-command-center/SKILL.md`. The dashboard must be re-published to claude.ai for viewers to get the new card.

[2026-07-26] DECISION: Added a "Save .md file" button to the command-center Email Performance Report (alongside Copy/Show Markdown), using the artifact `downloads` runtime capability (`window.claude.downloads.save`), with a graceful fallback (reveal Markdown) when `downloads` isn't granted. | REASONING: Forkan wanted the report saveable as a Markdown file, not just copyable. Raw `<a download>` is unreliable in the artifact sandbox, so the platform `downloads` capability is the correct path; `.md` is in the base allowlist. | CONTEXT: Code is live on artifact 6d56d543 (published content-only, carrying the `mcp` grant forward). Actual file-save stays in fallback until a publish with `capabilities:{mcp,downloads}` runs from a session that has the claude.ai Salesforce+Asana connectors — this Claude Code session uses local uuid-named MCP connectors, so restating the `mcp` manifest is rejected at publish time (can't add `downloads` without restating `mcp`, since capabilities is a full-set declaration). Verified JS with node --check. Documented in SKILL.md.

[2026-07-26] DECISION: Fixed the live command-center dashboard's "Save .md file" button (Email Performance Report card) — it did nothing because the published artifact declared only the `mcp` capability, so `window.claude.downloads` was undefined and the handler fell back to just revealing raw Markdown. Republished artifact 6d56d543 declaring BOTH `capabilities: {mcp:{servers:[SF:soqlQuery, Asana:get_tasks/get_task_stories/update_tasks]}, downloads:true}`. Also corrected the publish-capability docs in two places so it can't silently recur: the `dashboard-live.html` header comment and `mops-command-center/SKILL.md` line 167 (which had listed only `mcp` and a stale tool list) now both state that `downloads` must be restated on every publish because `capabilities` is a full-set declaration. | REASONING: The root cause was a prior republish carrying only `{mcp}` forward — `capabilities` is a full-set declaration, so an omitted capability is revoked; whoever last shipped the report feature dropped `downloads`. | CONTEXT: Cross-session gotcha caught mid-fix — a SEPARATE session had edited + republished this dashboard AFTER this session's bar-chart update, adding a whole new "Email performance report — full analysis" generator card (quarter/month/week selector + Copy/Show/Save Markdown). This session's scratchpad copy was stale (missing that feature, 932 vs 1299 lines); force-publishing it would have destroyed the report feature. Verified via WebFetch + diff, then republished the EXACT current live content (extracted from the fetched page) with only the capability added — nothing lost. Lesson: this artifact has concurrent editors; always WebFetch + diff the live version before republishing, never push a local/scratchpad copy blind. SF/Asana connector calls were not re-observed this session (publish warned) but no connector code changed. `.md` is in the downloads base allowlist; saves still require viewer confirmation.

[2026-07-28] DECISION: Added an "Intake — Jira channel" section to the command-center live dashboard (`dashboard-live.html`), treating the MOPS Jira project as a second intake front-door alongside Asana. Ships three features: (1) real-vs-noise backlog split, (2) orphaned-real alert (tickets on deactivated assignees), (3) aging watchlist by created-age. | REASONING: The dashboard read "Asana/Salesforce/Jira" but never actually queried Jira; other teams file real requests into `project = MOPS` (409 open), so the Asana-only view under-reported true intake. A full 409-issue profile showed only 289 are real ops work (120 are an automated privacy/DSAR stream), 188 real tickets are orphaned on deactivated users, and the backlog is ancient (229 real tickets 2yr+, oldest created 2019). | CONTEXT: Read-only, in-browser compute mirroring the SF/Asana pattern; loads independently with its own degrade path. Noise classifier keys on the automation account `5e163dff9af3650e9e40a3e4` + a privacy/DSAR summary regex. Deliberately does NOT use `updated` (bulk-reset project-wide → dead staleness signal) or `duedate` (0/409 populated) — aging uses `created`. Publishing now requires restating the `claude_ai_Atlassian` server (`searchJiraIssuesUsingJql`) in the `mcp` capability alongside Salesforce/Asana + `downloads`, or the connector silently drops. `computeJira` unit-validated against the live pull (reconciles 409/289/120/207). Snapshot `dashboard-template.html` does not carry this section.

[2026-07-28] DECISION: Added a per-row "Close" write action to the command-center live dashboard's Jira orphaned/aging tables (published to artifact 6d56d543), letting the manager close dead tickets from the dashboard. Wired but no ticket was closed in-session. | REASONING: The Jira aging/orphaned features surfaced ~380 stale, mostly-orphaned tickets whose only sensible action is closing; a dashboard Close button lets the manager clean them up directly instead of hand-editing Jira. | CONTEXT: Probed the MOPS Jira workflow first — it is forward-only/linear (To Do → In Progress → In Review → Done) and **Done is terminal: no reopen transition exists** (verified on closed Ticket + Sub-task issues, both zero transitions). So closes are irreversible from the connector; the button therefore warns one-way, requires a resolution (Done/Won't Do/Outdated = ids 10000/11010/6, the Done step's required field), chains transitions live via getTransitionsForJiraIssue, and does NOT auto-retry the transitionJiraIssue write. Chaining logic unit-checked (In Progress→In Review→Done). transitionJiraIssue was deliberately NOT fired in-session because it can't be undone — user chose "build the button, first real close made knowingly by the manager" (declined the throwaway test-close of MOPS-290933). Manifest now declares claude_ai_Atlassian tools searchJiraIssuesUsingJql + getTransitionsForJiraIssue + transitionJiraIssue; Salesforce/Asana query code carried forward unchanged (flagged unverified-in-session by the publisher, same as prior publish).
