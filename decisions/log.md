# Decision Log

Append-only. When a meaningful decision is made, log it here.

Format: [YYYY-MM-DD] DECISION: ... | REASONING: ... | CONTEXT: ...

---

[2026-07-08] DECISION: Built initial skill scaffolds for intake-classification, campaign-naming, sf-campaign-spec, and intake-tracking, with unconfirmed schema/convention details explicitly flagged rather than guessed. | REASONING: No campaign naming convention or Salesforce record type/status schema was confirmed yet; guessing silently would repeat the exact "naming drift" and "wrong status scaffolding" problems these skills are meant to fix. | CONTEXT: First skills built after initial MOPS assistant setup; refine each skill's placeholders as real Asana/Salesforce schema and a naming convention get confirmed through use.

[2026-07-08] DECISION: Campaign naming convention is `type_subtype_region_description_year_quarter` (e.g. `evt_ws_all_dam workshop boston_2026_q3`). | REASONING: Forkan confirmed this is the actual format to use, replacing the draft/no-convention state. | CONTEXT: Only `evt` (type), `ws` (subtype), and `all` (region) are confirmed codes so far — see `.claude/skills/campaign-naming/SKILL.md` for the growing code table.

[2026-07-10] DECISION: Campaign naming convention is superseded — now `Region_Channel_Product_Description_YYYY-Qn`, replacing the 2026-07-08 `type_subtype_region_description_year_quarter` format. | REASONING: Forkan confirmed this as the correct/current format while reviewing the MOps-weekly-report skill. | CONTEXT: Only `Region=All` carries over as confirmed; Channel and Product codes are unconfirmed under the new segment structure — ask rather than invent, per `.claude/skills/campaign-naming/SKILL.md`.

[2026-07-11] DECISION: Which intake requests become Salesforce Campaigns is determined by the Asana `[MOPs] Intake` project's `Project Type` custom field (gid `1206591746930193`) — confirmed campaign-producing values are `Webinar Request`, `Event (+ SFDC Campaign)`, `SFDC Campaign only (single)`, `SFDC Campaigns only (multiple)`, and `UTM(s) (+ SFDC Campaign)`; all other values (`Email(s) only`, `Audiences`, `Reporting`, `List Upload`, `Form Request`, `Other`, internal-ops types) do not. | REASONING: Cross-referenced ~100 recent completed Asana tasks against live Salesforce Campaign records by name — e.g. "Conductor - Partner Master Class" (Webinar Request) resolved to two real Campaigns (Webinar-type + Email-type), while "SCSM Customer Innovation Session Email Report" (Reporting) had no corresponding Campaign creation. | CONTEXT: Encoded into `.claude/skills/intake-classification/SKILL.md` (classification table) and `.claude/skills/sf-campaign-spec/SKILL.md` (gate before proposing a campaign, step 2 of Process).

[2026-07-13] DECISION: `List Upload` and `Form Request` are not unconditional "no Campaign" cases — before skipping, check whether a Campaign already exists for the named event/webinar. `List Upload` needs a new Campaign the ~30% of the time the event/webinar is partner-hosted with no existing Acquia Campaign; `Form Request` needs one the ~30% of the time the web team built the landing page ahead of any Campaign creation. | REASONING: Harish walked through both scenarios live in the 2026-07-13 meeting (`transcripts/Harish_Forkane meeting .txt`) — his manual process already makes this distinction, the 2026-07-11 table was too coarse. | CONTEXT: Updated `.claude/skills/intake-classification/SKILL.md` (table) and `.claude/skills/sf-campaign-spec/SKILL.md` (step 2 exception).

[2026-07-13] DECISION: Webinar/Event requests only get a companion Email-type Campaign when the stakeholder answers yes to a promotional-email flag ("do you require a promotional email as part of this request?") — not automatically. When created, the Event/Webinar Campaign is set as the Email Campaign's Parent Campaign in Salesforce, and both share the same name except the `Channel` segment. | REASONING: Harish demoed this exact manual pattern (parent campaign linkage) and clarified that not every webinar/event needs the email leg — it depends on whether promotional email was requested. | CONTEXT: `intake-classification/SKILL.md` step 4 (flag capture), `sf-campaign-spec/SKILL.md` steps 3-4 (trigger + parent linkage), `campaign-naming/SKILL.md` step 6 (naming pattern).

[2026-07-13] DECISION: Email channel naming gets a `Nurture` subtype, separate from the general `Email` code that covers all other one-off email types (promotional, post-webinar, post-event, etc). Exact short code not locked — Harish suggested `NURT` but said "or anything you find suitable." | REASONING: Harish distinguishes nurture-sequence requests from one-off email sends in his naming; wanted this reflected before the convention is treated as fully closed. | CONTEXT: `campaign-naming/SKILL.md` known-codes table — confirm the exact code with Harish before treating it as final. Also flagged: Salesforce's Campaign Name field has a hard character limit that stakeholder-provided description text can exceed (Harish's manual workaround was silent truncation); exact cap not finalized (40-50 chars suggested) — see same skill file, step 5.

[2026-07-13] DECISION: Multi-send email requests (e.g. 3-4 promotional emails for one webinar) get scheduled as individual dated Asana milestone sub-tasks — one per send, `Project Type` = `Email`, converted to a Milestone — forming a shared email send calendar used to catch same-day/same-audience clashes and to report sent/upcoming emails. | REASONING: This is Harish's existing manual process, demoed live; formalizing it as a skill avoids re-deriving the pattern each time and lets `MOps-weekly-report` query it consistently. | CONTEXT: New skill `.claude/skills/email-send-calendar/SKILL.md`; referenced from `intake-classification/SKILL.md` (step 5-6) and `MOps-weekly-report/SKILL.md` (new slides 5 and 12, added to the retro/look-ahead sections).

[2026-07-13] DECISION: Milestone creation mechanics are confirmed — pass `resource_subtype: "milestone"` directly on Asana task creation (`create_tasks`), and pass `custom_fields` as a JSON string (not a nested object) when setting fields like `Project Type` in the same call. | REASONING: Verified live against the `[MOPs] Intake` project by running the full pipeline (classification → naming → email-send-calendar) end-to-end on the "email campaign for a new AI web building assistant (demo)" task — the milestone sub-task was created successfully on the first attempt with these exact parameters. | CONTEXT: Removes the "unconfirmed mechanics" caveat previously in `email-send-calendar/SKILL.md` step 5. Same run also confirmed the `UTM(s) (+ SFDC Campaign)` `Project Type` option no longer exists in the live Asana enum (only the 15 values now in `intake-classification/SKILL.md`'s table are real) — worth reconciling that table next time it's touched.

[2026-07-20] DECISION: Building a manager-facing "MOps Command Center" — an agentic OS for Jennifer Schroback (Sr. Manager, Agentic Marketing Ops) — as a composition layer over existing skills, NOT a new codebase. Architecture: (1) a read-only `mops-command-center` skill that computes one `briefing` object from live Asana/Salesforce/Jira; (2) a live web dashboard (Artifact) as the single pane of glass; (3) a proactive routine (weekday morning brief + deduped daytime alerts to Slack). Chosen surface = live web dashboard; authority = read + Asana/Slack actions with confirmation; cadence = daily brief + alerts. | REASONING: The reporting skills (`intake-tracking`, `parbot-email-performance`, `MOps-weekly-report`) already pull this data but on separate cadences with no single surface and no proactive push between Thursday reviews; the manager wanted one always-current view. Hard constraint: Salesforce MCP is read/query-only, so the OS reports on campaigns/email but cannot create/edit SF records — writable actions limited to Asana/Slack/Jira. | CONTEXT: This session authored `.claude/skills/mops-command-center/SKILL.md` + `dashboard-template.html` (snapshot render target) and `routines/mops-command-center/README.md` (blueprint, not yet provisioned). Live-dashboard (`mcp` self-refresh) and Slack sends require an authorized interactive session — the `mcp` capability page can't be publicly shared and needs a real request/response observed per connector tool before shipping. Open items: Slack delivery target (DM vs. channel), whether the dashboard also serves Aayushi (lead), and whether the cloud routine env can publish an Artifact.

[2026-07-21] DECISION: Shipped the self-refreshing, Acquia-branded MOPS Command Center dashboard (`.claude/skills/mops-command-center/dashboard-live.html`), published to the same Artifact URL as the earlier snapshot. It declares the `mcp` runtime capability and calls the viewer's own Salesforce (`soqlQuery`) + Asana (`get_tasks`) connectors on each visit, paginating and computing the briefing in-browser (logic keyed off custom-field GIDs — Project Type 1206591746930193, Priority 1211656326879675, MOPS-Status 1210850678809573 — to avoid the trailing-space fragility on the "Priority " field name). | REASONING: The primary Salesforce/Asana connectors turned out to be authenticated in-session, so real data + real tool-response shapes were observable — the prerequisite for building live `mcp` calls. Compute logic was unit-tested in Node against the real page-1 task payload before publishing (open rate 9.60%, CTR 0.31%, −4.55pt delta reproduced exactly; workload/overdue/unclassified/null-priority all scaled correctly). Branding applied from the real Acquia system (navy #232C61 header, Sky Blue #26A3DD droplet motif + inlined wordmark, Acquia Blue #036BB5 titles, status colors Pink/Orange/Sky). | CONTEXT: Tradeoffs — an `mcp`-declaring page is PRIVATE per-viewer and cannot be shared by public link; each viewer (e.g. Jennifer) must have Salesforce + Asana connectors enabled in their own claude.ai. On-time % is deliberately shown as "—" (the 'Out of SLA/Rush' flag is unpopulated; needs a due-date-vs-completion measure). Per-section error handling branches on the mcp McpError codes (needs_reauth/server_not_connected/etc.). The browser `window.claude.mcp` runtime itself could NOT be tested from this CLI session — only the pure compute was verified — so the first real open should be sanity-checked. Snapshot form (`dashboard-template.html`) retained for the routine + shareable use.

[2026-07-21] DECISION: Added a "Pipeline-influenced $ on email" metric — created skill `.claude/skills/pipeline-influenced-email/SKILL.md` (single source of truth) and wired a pipeline section into the live command-center dashboard's email card. Metric = SUM(AmountAllOpportunities) + SUM(NumberOfOpportunities) + SUM(NumberOfWonOpportunities count) from Salesforce Campaign rollups, over email campaigns (TotalEmailsDelivered>0, Type!='Operational'); derived ratios $/1k-delivered and response→opp rate. Dashboard shows: Pipeline influenced $, opps, won count, $/1k sent, response→opp %, and a top-5-emails-by-pipeline table; updates with the quarter selector. | REASONING: Forkan asked whether email has a direct monetary result; live SFDC query confirmed AmountAllOpportunities is populated and meaningful (e.g. $99K on the top email), while won $ (AmountWonOpportunities) is unreliable (0s, a −$2,502) and ExpectedRevenue/ActualCost are null org-wide. So pipeline is reportable but won$/ROI are not. | CONTEXT: Attribution is single-touch (Primary Campaign Source) — undercounts email's assist role; true multi-touch would need the CampaignInfluence object (not yet checked/enabled). Pipeline lags the send, so recent windows look lower — compare like-for-like periods only; no period-over-period pipeline delta shown to avoid a false "decline." Amounts may be currency-converted — confirm single reporting currency before cross-region sums. All computed client-side in the dashboard from the same row-level SOQL used for engagement; verified in Node against realistic figures ($187K/126 opps/$4,785 per 1k). Footnote on the card states the attribution + data-gap caveats.

[2026-07-21] DECISION: Replaced the placeholder on-time tile ("—", based on the near-empty "Out of SLA/Rush" flag) with a REAL on-time SLA %: business-day turnaround (Asana created_at → completed_at) vs a per-Project-Type turnaround SLA, over the last 30 days of completions. Live validation returned ~33% on-time, avg 17.5 business days (Email 23% / SFDC single 11% / List Upload 80%) — consistent with the documented ~47%-ish reality, not the false 97% the flag gave. Implemented in the command-center dashboard (computeOnTime + bizDaysBetween) and defined in `mops-command-center/SKILL.md` Step 3.A. | REASONING: The "Out of SLA/Rush" flag is populated on ~1/35 completed tickets, so it can't measure on-time. created_at is the best submission proxy available. CRITICAL modeling call: the documented "Webinar 2 weeks out / Event 4 weeks out" are REQUESTOR lead-time-before-event rules, NOT MOps turnaround SLAs — scoring created→completed against them produced spurious 0%/28-day results, so Webinar/Event (and no-SLA types: Reporting, Audiences, Automations|Martech, Form Request, Other, UAT, Issues, 6Sense CE, Team OOO, unset) are EXCLUDED from the on-time %, counted separately. | CONTEXT: Turnaround SLA map (business days): SFDC single 1, SFDC multiple 2, UTM(+SFDC) 2, Email/Nurture 5, List Upload 7 (upper of 3-7), IT/Integration 15 (upper of 7-15). Business days ignore holidays; created_at = submission proxy. Dashboard now fetches incomplete backlog + 30-day completions in one paginated get_tasks (completed_since=30d ago) and splits them (avoids the search_tasks 100-cap). On-time-below-target alert fires under 80% (warn under 50%). Footnote on the card states the method + exclusions. Open follow-on: model event-date-based lead-time SLA for Webinar/Event separately if wanted.

[2026-07-21] DECISION: Aligned the on-time SLA model to the official "MOps Requests: SLA Timeline Requirements (2025)" doc (now saved at references/sops/mops-sla-timeline.md). Changes: ADDED Form Request = 5 bd and Reporting (Salesforce Reports) = 5 bd to the turnaround SLA map (both were previously excluded) — on-time recomputed to ~34% over 61 scored completions (Reporting 42%, Email 23%, SFDC single 11%, List Upload 80%). CONFIRMED the Webinar (2 wks out) / Event (4 wks out) exclusion: the doc explicitly anchors those to the live event date and counts the MOps deliverable chain from the 1st promotional send date — so a created→completed turnaround is the wrong measure; they stay excluded. | REASONING: Forkan surfaced the authoritative SLA doc; it validated the turnaround-vs-lead-time split and corrected two mappings I'd guessed as "no SLA." | CONTEXT: Documented approximations the Asana Project Type enum forces (in mops-command-center/SKILL.md Step 3.A): SFDC "multiple" → 2 bd though the doc's real breakpoint is >5 campaigns (enum lacks count); single-email 5 bd is also applied to multi-send requests whose true SLA is 8 bd (enum can't distinguish); UTMs-only (1 bd) has no enum value, only UTM(+SFDC)=2 bd. Still-excluded: Audiences, Automations|Martech, Other, UAT, Issues, 6Sense CE, Team OOO, Routing (7-15 bd but no enum value), unset. Other doc facts captured in the SOP for future use: multi-email 8 bd chain, deliverables checklist, 3-emails/week/audience cap, and the weekday send-ownership calendar (Mon webinars / Tue nurture / Wed ABM / Thu events / Fri rest) — a future dashboard enhancement could flag day-type mismatches and >3/week/audience directly.

[2026-07-21] DECISION: Shipped the first action-layer feature on the live command-center dashboard — an "Unassigned tickets" card with a per-row owner picker → inline confirm → Asana update_tasks write (assigns an owner). This turns the dashboard from read-only into a tool the team acts from, targeting the biggest lever (60 unassigned). | REASONING: Forkan approved building the Assign action first. Verified the write path by observing a REAL update_tasks request/response in-session (a no-op self-assign on my own "Task 1", assignee="me", zero change) — response shape is {data:[{gid,name}], failed:[], summary}, so success = failed[] empty. Looked up the team's Asana user GIDs (Harish 1203937989075417, Aayushi 1212534096012004, Felipe 1214552680652024, Jennifer 1214047208203035, Forkane 1215719015567131) for the picker. | CONTEXT: Added "update_tasks" to the page's mcp manifest (Asana tools now get_tasks + update_tasks). Writes run with the VIEWER's credentials (properly attributed). Guardrails: inline confirm before every write (no native confirm — sandbox-safe), additive-only (assign, no deletes), and "maybe it ran" handling for server_unavailable/upstream_error/cancelled (no auto-retry; a ↺ reset lets the user re-pick as a fresh gesture). Salesforce stays read-only. Could NOT exercise the in-browser click path from the CLI — only the API-shape write was verified — so the first real assignment should be sanity-checked on a test ticket. Next action-layer candidates: Set Priority (136 null), Nudge owner (Slack DM), both following this same confirm→callTool→toast pattern.

[2026-07-23] DECISION: The Google Drive MCP's `create_file` tool cannot be used to upload a finished multi-slide `.pptx` (e.g. the ~600KB, 19-slide MOps Weekly Review deck) — its only content parameter is inline `base64Content`, which the calling model must literally generate as tool-call output; a 600KB file is ~800K base64 characters (base64 tokenizes close to 1 token/char), far past any single turn's output budget. Confirmed via a dedicated subagent that hit this wall even after chunked reads. | REASONING: This is a hard token-generation ceiling, not a retryable error or an auth/size-limit on Drive's side — no amount of file-size optimization on a real styled 19-slide deck gets it under the wall, since pptx has a non-trivial fixed floor (theme/master/relationships) even before charts/tables. | CONTEXT: `MOps-weekly-report/SKILL.md`'s Output step ("upload the .pptx via Google Drive create_file") is currently not achievable end-to-end for a deck this size — the 2026-07-23 run built the full deck locally, flagged the gap on the #mops-team Slack post per the skill's Delivery contingency, and hand-delivered the `.pptx` file directly instead. Needs either an upload path that accepts a file handle/path (not inline base64) or a much smaller deck format before this step can be automated — flagging for follow-up rather than re-attempting the same approach next week.

[2026-07-26] DECISION: Converted the command-center dashboard's magnitude-by-category metrics from thin horizontal progress-fill "bars" (which read as plain lines) to proper vertical bar charts. Applies to all four such metrics: Capacity Workload (open tasks by person), Ticket status by stage, Ticket aging buckets, and Triage-lag distribution — in both `dashboard-live.html` and (for Workload, the only one it renders) `dashboard-template.html`. | REASONING: Forkan asked for bar charts instead of "just a plain line" on Capacity Workload "and the similar metrics." Followed the `dataviz` skill: form = bars for magnitude-by-category; single hue (sky→blue) with a warning accent (orange→pink) for flagged categories (Unassigned / Waiting-for-Feedback+Incomplete / 1–3mo+3mo+ / >3 business days) — no categorical palette, so no ΔE validator run needed; baseline-anchored 4px-rounded tops, direct value labels above each bar, per-bar hover (title + brightness), theme-aware via existing CSS vars. | CONTEXT: Added a reusable `barChart(items,{empty})` helper + `.vbars` CSS to both files (128px plot, 20px headroom so the tallest bar's value label isn't clipped, zero values keep a 3px baseline stub, >5 bars → dense gap) and a `shortName()` for compact person labels (full name kept in the hover title). Removed the now-unused `.bar/.track/.fill` CSS. Logic verified in Node (height scaling, name-shortening, empty/all-zero cases, HTML escaping); the in-browser render itself wasn't screenshot-tested from the CLI. KPI stat tiles (email open/CTR/sends, pipeline) were left as tiles — they're single headline numbers, not category series.

[2026-07-26] DECISION: Added an "Email Performance Report" generator to the mops-command-center live dashboard (`dashboard-live.html`) — on-demand full email-performance brief for any quarter/month/week, computed client-side from one live Salesforce Campaign pull, with a Copy-Markdown export in the exact written-brief format. | REASONING: The manager/marketing team needed reproducible, reliable email reports (same structure/format as the hand-written Q2 2026 brief and the parbot deck) with a selectable timeline, not just the fixed trailing-30/quarter tile the dashboard already had. Computing on-screen and export from the same row set guarantees they reconcile; using the Campaign rollup `AmountAllOpportunities` keeps the number equal to the `pipeline-influenced-email` single-source metric the brief used. | CONTEXT: Verified in node against the live Q2 2026 pull — reconciled to 78 campaigns / 1,089,367 delivered / 95,472 opens / 5,555 clicks / $7,386,728 pipeline; cohorts, top lists, red flags matched the standalone Python analysis. Live surface only (needs the viewer's Salesforce connector); read-only. Documented in `.claude/skills/mops-command-center/SKILL.md`. The dashboard must be re-published to claude.ai for viewers to get the new card.

[2026-07-26] DECISION: Added a "Save .md file" button to the command-center Email Performance Report (alongside Copy/Show Markdown), using the artifact `downloads` runtime capability (`window.claude.downloads.save`), with a graceful fallback (reveal Markdown) when `downloads` isn't granted. | REASONING: Forkan wanted the report saveable as a Markdown file, not just copyable. Raw `<a download>` is unreliable in the artifact sandbox, so the platform `downloads` capability is the correct path; `.md` is in the base allowlist. | CONTEXT: Code is live on artifact 6d56d543 (published content-only, carrying the `mcp` grant forward). Actual file-save stays in fallback until a publish with `capabilities:{mcp,downloads}` runs from a session that has the claude.ai Salesforce+Asana connectors — this Claude Code session uses local uuid-named MCP connectors, so restating the `mcp` manifest is rejected at publish time (can't add `downloads` without restating `mcp`, since capabilities is a full-set declaration). Verified JS with node --check. Documented in SKILL.md.

[2026-07-26] DECISION: Fixed the live command-center dashboard's "Save .md file" button (Email Performance Report card) — it did nothing because the published artifact declared only the `mcp` capability, so `window.claude.downloads` was undefined and the handler fell back to just revealing raw Markdown. Republished artifact 6d56d543 declaring BOTH `capabilities: {mcp:{servers:[SF:soqlQuery, Asana:get_tasks/get_task_stories/update_tasks]}, downloads:true}`. Also corrected the publish-capability docs in two places so it can't silently recur: the `dashboard-live.html` header comment and `mops-command-center/SKILL.md` line 167 (which had listed only `mcp` and a stale tool list) now both state that `downloads` must be restated on every publish because `capabilities` is a full-set declaration. | REASONING: The root cause was a prior republish carrying only `{mcp}` forward — `capabilities` is a full-set declaration, so an omitted capability is revoked; whoever last shipped the report feature dropped `downloads`. | CONTEXT: Cross-session gotcha caught mid-fix — a SEPARATE session had edited + republished this dashboard AFTER this session's bar-chart update, adding a whole new "Email performance report — full analysis" generator card (quarter/month/week selector + Copy/Show/Save Markdown). This session's scratchpad copy was stale (missing that feature, 932 vs 1299 lines); force-publishing it would have destroyed the report feature. Verified via WebFetch + diff, then republished the EXACT current live content (extracted from the fetched page) with only the capability added — nothing lost. Lesson: this artifact has concurrent editors; always WebFetch + diff the live version before republishing, never push a local/scratchpad copy blind. SF/Asana connector calls were not re-observed this session (publish warned) but no connector code changed. `.md` is in the downloads base allowlist; saves still require viewer confirmation.

[2026-07-27] DECISION: Built three manager-facing triage/enforcement skills — `intake-routing`, `campaign-hygiene-audit`, `sla-watchdog` — in `.claude/skills/` (not a separate `agents/` folder; user chose to keep them as normal invocable skills). They target the connective-tissue gaps the existing intake pipeline and reporting skills don't cover, each grounded in a documented MOps-vault insight. | REASONING: (1) `intake-routing` closes three insights at once — no routing (ownership sat in the static 2026 Team Structure table, applied by hand), Priority null on nearly all new tasks, and Harish/Aayushi workload imbalance; runs after `intake-classification` to assign owner (Region×Project Group×Stakeholder, first-match-wins rules) + derive Priority from the SLA clock + surface load without silently overriding the ownership model. (2) `campaign-hygiene-audit` is a read-only SF auditor (safe given SF is query-only) for the documented drift — 1,938 blank `Type`, missing Region, a third naming convention found live, member-status/sync mismatches — outputting fix specs, never a completion claim. (3) `sla-watchdog` operationalizes the ~47%-on-time gap: watches each open ticket against its per-type SLA and escalates before breach; it is now the single source of the per-type SLA table (`intake-routing` priority + `mops-command-center` read it from here). | CONTEXT: All three cross-reference `.claude/rules/write-actions.md` (routing assign = Class B notifies; Slack alerts = Class C; SF audit = §9 spec+handoff) and existing skills (`campaign-naming` codes, `sf-campaign-spec` member statuses, vault `campaign-taxonomy`). Two skills are routine-shaped (hygiene weekly, watchdog daily) but built as skills first — routine provisioning + any unattended Slack send still needs a §6 authorization row (only the weekly review is standing-authorized). Known soft spots to confirm with the team: the Project Type → Project Group bridge in `intake-routing` is inferred not team-confirmed; the Asana Priority picklist values need verifying before writing; the two SLA copies (MOPAS vs raw doc) may have drifted. Source: this session's strategic review of the vault insights backlog + documented SLAs, following the `MOps Team Structure 2026` doc created earlier in-session.

[2026-07-28] DECISION: Added an "Intake — Jira channel" section to the command-center live dashboard (`dashboard-live.html`), treating the MOPS Jira project as a second intake front-door alongside Asana. Ships three features: (1) real-vs-noise backlog split, (2) orphaned-real alert (tickets on deactivated assignees), (3) aging watchlist by created-age. | REASONING: The dashboard read "Asana/Salesforce/Jira" but never actually queried Jira; other teams file real requests into `project = MOPS` (409 open), so the Asana-only view under-reported true intake. A full 409-issue profile showed only 289 are real ops work (120 are an automated privacy/DSAR stream), 188 real tickets are orphaned on deactivated users, and the backlog is ancient (229 real tickets 2yr+, oldest created 2019). | CONTEXT: Read-only, in-browser compute mirroring the SF/Asana pattern; loads independently with its own degrade path. Noise classifier keys on the automation account `5e163dff9af3650e9e40a3e4` + a privacy/DSAR summary regex. Deliberately does NOT use `updated` (bulk-reset project-wide → dead staleness signal) or `duedate` (0/409 populated) — aging uses `created`. Publishing now requires restating the `claude_ai_Atlassian` server (`searchJiraIssuesUsingJql`) in the `mcp` capability alongside Salesforce/Asana + `downloads`, or the connector silently drops. `computeJira` unit-validated against the live pull (reconciles 409/289/120/207). Snapshot `dashboard-template.html` does not carry this section.

[2026-07-28] DECISION: Added a per-row "Close" write action to the command-center live dashboard's Jira orphaned/aging tables (published to artifact 6d56d543), letting the manager close dead tickets from the dashboard. Wired but no ticket was closed in-session. | REASONING: The Jira aging/orphaned features surfaced ~380 stale, mostly-orphaned tickets whose only sensible action is closing; a dashboard Close button lets the manager clean them up directly instead of hand-editing Jira. | CONTEXT: Probed the MOPS Jira workflow first — it is forward-only/linear (To Do → In Progress → In Review → Done) and **Done is terminal: no reopen transition exists** (verified on closed Ticket + Sub-task issues, both zero transitions). So closes are irreversible from the connector; the button therefore warns one-way, requires a resolution (Done/Won't Do/Outdated = ids 10000/11010/6, the Done step's required field), chains transitions live via getTransitionsForJiraIssue, and does NOT auto-retry the transitionJiraIssue write. Chaining logic unit-checked (In Progress→In Review→Done). transitionJiraIssue was deliberately NOT fired in-session because it can't be undone — user chose "build the button, first real close made knowingly by the manager" (declined the throwaway test-close of MOPS-290933). Manifest now declares claude_ai_Atlassian tools searchJiraIssuesUsingJql + getTransitionsForJiraIssue + transitionJiraIssue; Salesforce/Asana query code carried forward unchanged (flagged unverified-in-session by the publisher, same as prior publish).

[2026-07-28] DECISION: Created a single standing write contract — `.claude/rules/write-actions.md` — governing every write the assistant performs (Asana, Jira, Confluence, Slack, Drive) regardless of entry point (chat, skill, dashboard button, routine), plus an append-only audit trail at `decisions/actions.md`. Contract defines: a closed verb registry per system; three reversibility classes (A reversible / B notifying / C terminal) driving the confirmation requirement; a bulk policy (1-3 normal confirm, 4+ preview-then-one-batch-approval with per-record results, Class C never batch-approved, hard stop at 25); per-verb idempotency checks before writing; a never-auto-retry failure policy splitting "may have applied" MCP error codes (server_unavailable/upstream_error/cancelled/timeout -> report AMBIGUOUS and read back) from "definitely didn't apply" (needs_reauth/server_not_connected/validation); an unattended-write authorization table; attribution; and §9 spec-plus-handoff handling for systems with no write path. | REASONING: Writes were happening from six different sites (two dashboard buttons, intake-classification, email-send-calendar, MOps-weekly-report, the weekly-review routine, ad-hoc chat) with inconsistent and mostly undocumented guardrails — the dashboard buttons confirmed and never auto-retried, the skills specified neither, and chat did whatever seemed reasonable. The only standing rule touching writes (`.claude/rules/mops-operations.md`) said to "create or update records" with no guardrail attached. No write was logged anywhere, no skill checked for duplicates before creating, and Slack authorization was renegotiated per feature. Same precedent as `campaign-gate-check`: one shared definition instead of each site re-deriving it. Most of the content was extracted from the two dashboard buttons, which already got it right. | CONTEXT: Three notable findings encoded while writing it — (1) **Slack sends are Class C**: no delete-message or cancel-scheduled-message tool is exposed, so the assistant cannot unsend, making multi-channel alerting the highest-blast-radius capability in the project; (2) **every MOPS Jira transition is Class C**, not just the close — the workflow is forward-only/linear with no backward hop, so even To Do -> In Progress can't be reverted through the connector; (3) **cloud routines do not inherit `.claude/rules/`** — they clone skill folders, so each write-performing routine repo must ship a copy of the contract or it runs unguarded (now stated in all three routine READMEs). Wired in at: CLAUDE.md (new "## Write Actions" section + actions.md in the Decision Log section), `.claude/rules/mops-operations.md` (replaced the bare "create or update records" line, added the Salesforce query-only constraint), and pointers in intake-classification, email-send-calendar, sf-campaign-spec (§9 spec-plus-handoff, logs nothing), MOps-weekly-report, and mops-command-center. Only ONE standing unattended authorization exists (§6): the weekly review post to #mops-team, de facto in production via `mops-weekly-review-2.0`. The intake-pipeline routine's Slack summary and the command-center brief/alerts are explicitly NOT covered and each need an approved §6 row before they can send unattended — flagged in their READMEs. Open follow-on: the contract is written but unexercised — no write has been performed under it, and the dashboard's Assign/Close buttons have still never fired in a browser.

[2026-07-30] DECISION: Confirmed the 2026-07-23 Google Drive upload blocker is NOT a file-size problem — rebuilt this week's 19-slide deck from scratch using only native pptx shapes (no embedded images/bitmaps), landing at 68KB, essentially the same size as the one deck known to have made it into Drive (`MOps_Weekly_Review_20260704.pptx`, 67KB — which on inspection was uploaded by Forkan directly, not by this routine). Reading that 68KB file's base64 back into context showed ~3.85 tokens per base64 character (86,384 tokens for a 22,418-character slice), meaning the full ~91K-character base64 payload would need roughly 350K tokens to emit as a single tool-call argument — still far past any single-turn output budget. | REASONING: This rules out "make the deck smaller" as a fix; base64 text tokenizes far more densely than plain prose in this tokenizer, so the ceiling is hit well before file size becomes the limiting factor. A real fix needs a different upload mechanism (chunked/resumable Drive upload, a Slides-native build API, or an upload tool that reads from a file path instead of requiring inline base64 in the call) rather than continued deck-size optimization. | CONTEXT: `MOps-weekly-report/SKILL.md` Output step is still not automatable end-to-end. This is the second consecutive run confirming the same root cause with more precision — recommend treating this as a standing infrastructure gap to raise with whoever owns the Drive/Slack MCP connectors, rather than re-attempting file-size tricks in future runs. 2026-07-30 run again hand-delivered the `.pptx` directly and flagged the gap on #mops-team.

[2026-08-03] DECISION: Built `lead-routing-audit` — a read-only daily QA skill for **Salesforce lead routing** (the sales-side engine; unrelated to `intake-routing`, which assigns Asana intake tickets to MOPs staff), plus a 12-check config linter for the rule table. Buckets each day's leads into 1 routed-properly / 2 wrong-rep / 3 didn't-route / 4 bypassed-by-design (evaluated 4→3→2→1, first match wins, one row per lead, sub-signals as columns). Delivery is one live Google Sheet (4 tabs: Daily Summary / Detail / Config Linter / Reference) appended daily via the local `gws sheets` CLI, reviewed each morning by Forkan + Lucio (RevOps). | REASONING: The team reported leads routing to the wrong reps for weeks with no detection except a rep complaining. Recon found the problem is fully auditable: routing is 100% custom (standard SF `AssignmentRule` for Lead returns 0 rows), the rules live in a queryable 76-row `Lead_Routing_Rule__c` table (43 active), and every routed lead is stamped with the rule that fired (`Lead_Routing_Rule__c` + `Lead_Routed_Date__c`) — so expected owner is independently recomputable and diffable, making this a real audit rather than a heuristic. Volume is ~11 leads/day in scope, small enough for a genuine daily human review. | CONTEXT: Four findings shaped the design, all verified live. (1) **Root cause found, and it is a config bug, not drift**: the rule table has two generations, and the migration to the active set lost the region catch-alls — `1401`/`2200` are country-restricted to `DK;GB;IE;NO;SE`, and `525`/`601`/`1825` kept `Account_Type__c` populated so they only fire for leads that HAVE an account, the opposite of an "Unmatched" rule's purpose. Result: no-account leads in continental EMEA, MEA, non-Japan APJ and LATAM match NO active rule and fall to order 1001 (`RR AMER BDRs`). Confirmed on 7 live leads (KR, ID x2, NZ, PH, AU, BR). LATAM variant: LATAM leads carry `Region__c='Americas'` so they match AMER rules, meaning no-account BR/MX prospects reach NA BDRs instead of Livia Russo (`BDR LatAm`). Also found: 5 unreachable rules (525, 601, 783, 1825, 1850 — each criteria-identical to a lower-`Order_by__c` twin), 4 active rules pointing at deactivated users (Yoshi Han x3, Noriyuki Ishii; 14,636 open leads carry `Assigned_Inactive_User__c`), and order 783 tagged `Region__c='Americas'` despite being the EMEA CC rule. Fix spec (10 one-field edits) in `outputs/Lead_Routing_Config_Audit_2026-08-03.md` — SF is query-only so this is §9 spec-plus-handoff, no write claimed. (2) **Current `OwnerId` is not the routing outcome** — 8 of 40 sampled routed leads had since been DQ'd into `Leads - DQ/Recycle Queue` by the rep they were correctly routed to. The audit therefore compares against a *router-set owner* reconstructed from `LeadHistory` (`Field='Owner'` AND `CreatedBy.Name='B2BMA Integration'` AND `OldValue='00G6g000003ESwpEAG'`, earliest match; history is double-logged as names + Ids, dedupe to the Id row). This is the load-bearing mechanic and the most breakable piece. (3) **Round-robin rep selection is NOT verifiable** — 64% of routed MQLs go through RR, no rotation-pointer object exists, `Queue_Name__c` is null on all 43 active rules, and the rule->pool link is absent. The skill asserts pool/region consistency only and says so prominently rather than implying false precision. (4) **Partner Deal Registration bypasses the engine by design** (79 of 87 "unrouted" MQLs) — given its own bucket 4 evaluated first, otherwise bucket 3 would be ~91% false positives. Query constraints encoded as hard rules: never `getObjectSchema('Lead')` (300 fields/132KB, blows the token cap); never filter `LeadHistory` by `CreatedDate` (times out even at LAST_N_DAYS:1 LIMIT 10 — use a `LeadId` semi-join); never GROUP BY the 1300-char formula fields (`Region__c`, `Business_Segment__c`, `AVP_Geography__c`, `Account_Id__c`) or `Lead_Routing_Rule__r.Order_by__c`; never trust `Routing_MQL_Age__c`/`MQL_Age__c`. Nine assumptions remain unconfirmed with the routing owner (listed in `reference.md` §9) — chief among them whether order 1001 is *intentionally* the global fallback. Deliberately deferred: a Phase 0 calibration backtest over 30 days before the first Sheet append, so a systematic divergence becomes a Config Linter class rather than daily crying-wolf.

[2026-08-03] DECISION: Extended `.claude/rules/write-actions.md` §1 with a **Google Sheets** verb block (via the `gws` CLI, not MCP) — append/update/create = Class A, but overwriting or clearing a range a human edits = Class C — plus a §4 idempotency row for Sheet appends (natural-key dedupe; a day already logged makes the whole run a no-op). Updated `tools/available-tools.md` (stale since 2026-07-08) to document the `gws` CLI as the widest Google surface available and to correct the Drive MCP entry. | REASONING: `lead-routing-audit` is the project's first skill to write to Google Sheets, and the contract had no Sheets row at all — an unregistered verb means an unguarded write. The Class A/C split matters because the QA Sheet is co-edited: the skill owns columns A:X, the two reviewer-annotation columns are human-owned, and an `update` landing on those destroys work no connector can restore. | CONTEXT: There is **no Google Sheets MCP connector** and none in the registry (searched), so Sheets is reachable only through the local `gws` CLI, which is authed against Forkan's own keyring. Consequence now documented in three places: a cloud routine environment has neither the binary nor the credentials, so any Sheets-writing routine must run locally or be given its own — which is precisely why `lead-routing-audit` is hosted as a local launchd job rather than a cloud routine like `mops-weekly-review-2.0`. §6 (unattended authorization) was deliberately NOT extended: the weekly `#mops-team` post remains the only standing unattended write, so until a §6 row is added and logged, the skill produces rows for a human to paste.

[2026-08-03] DECISION: Provisioned the `lead-routing-audit` delivery surface — created the live Google Sheet **"Lead Routing QA — Daily (MOPs + RevOps)"** (`1gFu34xeJJ2Jrk1p9jtvgkzp_WtspHNyCGZ1zorbNNOA`, 4 tabs), seeded the human-owned `Reference` tab, populated `Config Linter` with 23 findings, ran the Phase 0 calibration backtest, ran one real report day end-to-end (2026-07-31), and wrote a **local launchd plist that is deliberately left unloaded**. | REASONING: The plan called for Phase 0 to run *before* the first append, precisely so a systematic recompute error becomes a config finding rather than daily crying-wolf. That turned out to be the load-bearing step: a literal reading of the rule table produces a **23.9% false rule-divergence rate**, above the 20% "systematic" threshold — the sheet would have been ignored by week two. | CONTEXT: The backtest (226 rule-stamped MQLs / 30 days, collapsed into 99 distinct criteria groups, so the recompute is evaluated exactly rather than sampled) forced three corrections to the skill. (1) **The recompute must return a chain of acceptable rules, not one expected rule.** Two engine behaviours aren't expressible in `Lead_Routing_Rule__c`: an `Account Team Member` rule **falls through** to the next matching rule when the account has no member with that `TeamMemberRole` (8.4% of leads — the Team-Member-Not-Found queue holds only 4 leads org-wide, consistent with fall-through being normal), and `Account__c` **may have been linked after routing** since it isn't history-tracked (8.8%). Modelling both drops false divergence to 4.0% and puts every sub-signal under the threshold, so all stay as per-lead rows. The proof that five declarative dimensions are insufficient: two criteria groups **identical on all five** fired **different rules** (780 for 6 leads, 90 for 4) — so a divergence means the engine disagreed with a literal reading of the table, never that the engine is provably wrong. (2) **The routing stamp is not always written by `B2BMA Integration`** — the Flow runs in whatever user context triggered the MQL. On lead `00QPb00001j5qI6MAI` the stamp was written by Jill Krueger (Marketing Manager) with `OldValue` a user rather than the Marketing Queue; the documented fingerprint matched zero rows and the "use current OwnerId" fallback would have reported `OWNER_MISMATCH`/High, blaming the router for a human's deliberate reassignment 14 seconds later. Extraction now matches on proximity to `Lead_Routed_Date__c` first and actor second; re-verified 4/4 on the regression set including the no-history fallback path. (3) **NEW High-severity config bug:** rules **70** and **1722** set `Account_Team_Role__c = 'Monsido Account Executive'`, which matches **zero** of the 16 real `TeamMemberRole` values org-wide — almost certainly a rebrand casualty (`Web Governance Sales Advisor` exists at 122,569 rows, ~1:1 with `DAM Sales Advisor`). Both rules can never route anyone, so Monsido leads carrying an account fall through to the generic account owner and never reach a Monsido AE. `reference.md` check 10 previously recorded "0 of 4" and has been corrected, along with a §10 regression case whose stated mechanism conflated `User.UserRole.Name` with `AccountTeamMember.TeamMemberRole` (right bucket, wrong reason, wrong flag). Everything else the linter re-derived from scratch matched the documented findings exactly (4 inactive-user rules, 5 criteria-identical shadowed rules, order 783's region contradiction, the EMEA/APJ/LATAM coverage gap). The real day ran to exactly the predicted ~11 leads (8 Partner Deal Registration, 2 routed OK, 1 wrong rep, 0 not routed), and idempotency was proven by a deliberate second run: no-op, zero duplicates, reviewer annotation intact. | DEFERRED: §6 unattended-write authorization was **not** added — that is Forkan's authorization to give, not the assistant's to grant, so the plist ships unloaded and the skill runs attended only. Also still open: sharing the sheet with Lucio (a permission change isn't mine to make), the 10 questions on the `Reference` tab, and 3-5 attended runs walked through with Lucio before the schedule goes on.

[2026-08-03] DECISION: Pinned the **bucket-3 severity mapping** in `lead-routing-audit` — a not-routed lead is **High** when nobody is accountable for it (no routed date + no rule stamped · an MQL stuck in `Leads - Marketing Queue` · owner is `Marketing Leads Routing Fallback` · owner is `Leads Queue - Team Member Not Found`) and **Medium** when it sits in `Leads - 72 hour lead re-route`. Also documented the whole High/Medium/Low ladder as a `SEVERITY` section on the Sheet's `Reference` tab so the reviewers can see and adjust it without a code change. | REASONING: The skill defined severity precisely for bucket 2's sub-signals but never stated what `Severity` a bucket-3 row gets — "High confidence" in the bucket definition describes the *detection* (direct field states), not the triage weight. Bucket 3 was empty on the one verified day (2026-07-31), so the gap was never exercised and would have fallen to per-run model judgment, which is exactly the kind of thing that drifts between runs and makes a shared sheet untrustworthy. | CONTEXT: The dividing line is accountability, not detection confidence. A qualified lead with no owner at all is the worst outcome the sheet can surface, so it outranks everything. `Leads - 72 hour lead re-route` is the one failure queue that is itself the escalation path — the SLA breach already happened and the re-route mechanism is engaged, so the lead is tracked rather than dropped, which makes it Medium. `Leads Queue - Team Member Not Found` stays High specifically *because* of the Phase 0 ATM fall-through finding: the engine normally walks past a team-member miss, so a lead actually landing in that queue means fall-through did not save it — rare, and unwatched at 4 leads org-wide. Bucket 1 and bucket 4 are now explicitly always Low, and the explanatory-only flags (`RULE_DIVERGENCE`, `ATM_FALLTHROUGH`, `ACCOUNT_LINKED_AFTER_ROUTING`, `ACCOUNT_OWNER_REGION_MISMATCH`, `ROUTER_STAMP_NOT_B2BMA`) are documented as never escalating. Severity now sums to `Leads_In_Scope` exactly as the bucket counts do.

[2026-08-03] DECISION: **Enabled the `lead-routing-audit` schedule** — installed and loaded the launchd agent `com.acquia.mops.lead-routing-audit`, running **05:30 America/Los_Angeles every day** and auditing **the previous day**. Added the corresponding **§6 unattended-write authorization** to `.claude/rules/write-actions.md`, scoped to this one Sheet and to `Detail!A:X` + `'Daily Summary'!A:N` + `'Config Linter'` only — `Detail!Y:Z` (reviewer annotations) and the `Reference` tab stay human-owned and are never written unattended. | REASONING: Forkan asked for it to run every morning at 5:30 LA time, which is the authorization the §6 row records. Three implementation choices were mine and are worth stating. (1) **Scope is `yesterday`, not `today`** — at 05:30 the current day holds almost nothing, so a morning review needs the last complete day; `Run_Date` is the report day, never the execution day. This also means the skill's default scope changed from `today` to `yesterday`. (2) **All seven days**, matching "every morning" literally: weekend volume is low but non-zero, and an unbroken `Run_Date` series makes a missing date an unambiguous missed-run signal rather than "was that a weekday?". (3) launchd `StartCalendarInterval` uses **system local time** and this Mac is on America/Los_Angeles, so 05:30 is 05:30 LA and follows DST automatically — documented as the thing to re-check if the machine's timezone ever changes, since there is no way to pin a timezone in the plist. | CONTEXT: This deliberately **skips the 3-5 attended runs** the original plan and README listed as a prerequisite, at Forkan's direction. The first week of rows is therefore the calibration period rather than a proven baseline — flagged in the README, and it matters most for bucket 3's severity mapping, which was pinned earlier today but has never fired against real data (0 bucket-3 leads on the one verified day). Also still outstanding and now more urgent because the routine is writing daily: the Sheet is **still private to Forkan** and needs sharing with Lucio manually, since a permission change isn't the assistant's to make. Verified after loading: plist lints, `claude` resolves on the login-shell PATH, 7 calendar entries registered, and the agent was kickstarted once as a live smoke test rather than trusting an unexercised cron entry.

[2026-08-03] DECISION: Built `references/sops/mops-task-subtask-catalog.md` — the per-`Project Type` sub-task catalog, mined from live Asana rather than authored — and **moved sub-task expansion out of `intake-classification`'s gate table into it**. The skill's step 6 now points at the catalog; its table is retitled "Known request types → SF campaign gate" and explicitly marked as not a sub-task list. | REASONING: Step 6 said "expand into sub-tasks using the mapping below," but the mapping only had a real sub-task list for **1 of 15** Project Types (`Webinar Request`), and that one was flagged "observed, not MOPS-confirmed." The other 14 rows carried campaign-gate notes, not deliverables — so for 93% of intake, the instruction pointed at nothing, and line 52 correctly forbade inventing a list. The result was an automation step that could only ever run for webinars, on unconfirmed data. Mining completed tickets makes it evidence rather than assertion, and separating "what work items exist" from "does this need a Campaign" stops one table serving two unrelated purposes. | CONTEXT: Method — for each of the 14 active `Project Type` options, pulled the 12–15 most-recently-completed **parent** tasks from `[MOPs] Intake` (gid `1205660951274722`, 2,664 completed) with `num_subtasks`, then deep-read the 12 richest parents for actual sub-task names. Findings: (1) **`Webinar Request` is a genuine 20-step template** — four samples (18–21 sub-tasks) share a near-identical ordered set, upgrading the old "observed" list to confirmed and adding the steps it missed (planning doc, events calendar, Zoom edit, stakeholder copy review, push-page-live/confirm-go-live split, social copy, on-demand conversion). Its `Webinar Email Creation` step **nests a second level** (promo emails 1–3 + attendee/no-show + report upload) — the old flat list hid this. A **3rd-party-hosted variant** skips Zoom/dry-run/hosting, so host must be checked before expanding those three. (2) **`Event (+ SFDC Campaign)` is a *different* 16-step template**, not "the same general shape as Webinar" as the skill claimed — no Zoom, no dry run, no on-demand; instead pre/post-event email, Know-Before-You-Go, field update, post-event summary, staff feedback. (3) **A third, undocumented pattern exists** — workshops/dinners/user groups run "SFDC campaign + form + N×(email creation / approval / send)", and the two DAM Workshop instances are byte-identical yet filed under *different* Project Types (`Event` vs `Form Request`), which corrupts per-type SLA and volume reporting. (4) **Seven types are leaves** where creating sub-tasks is actively wrong (`Issues` 12/12 zero, `SFDC Campaign only (single)` 12/15, `Audiences`, `Reporting`, `Form Request`, `Other`, `SFDC Campaigns only (multiple)`); the non-zero exceptions are mostly *misclassified* tickets, not richer work. (5) **`List Upload` is the most consistent type on the board** — exactly 1 sub-task, 12/15, one per list batch. (6) `IT/Integration` / `Automations | Martech` / `UAT` are ad-hoc project work with no pattern; `UAT` has **one** completed task ever. The catalog encodes these as five **evidence tiers** (Template / Template-inconsistently-applied / Formulaic / Leaf / Ad-hoc) so the automation knows when it may expand, when it must propose, and when it must create nothing — the distinction the old table couldn't express. Four field-level corrections also fell out and are recorded in catalog §7: `Project Type` has **17** options not 15 (`6Sense CE`, disabled, was missing); `UTM(s) (+ SFDC Campaign)` is **confirmed disabled**, settling that row's standing open question; an undocumented **`MOPs - Project Subtype`** field exists (gid `1211571206556215`: UTMs / SFDC ID / Email Send / Landing Page / Email) that no skill references and that likely resolves several `Other`/`Form Request` ambiguities; and an **`Out of SLA/Rush`** Yes/No field exists (gid `1211856242548764`) that `sla-watchdog` currently ignores in favour of deriving rush status from the SOP. Most operationally important single detail: sub-task **`resource_subtype` is load-bearing** — sends are created as `milestone`, approvals as `approval`; a send created as `default_task` never reaches the email send calendar. | DEFERRED: Four questions for Harish/Aayushi are listed in catalog §8 and are **not** resolved — chiefly whether the 20-step webinar set comes from an Asana **project template** (in which case the template, not this file, is source of truth), and whether the workshop pattern deserves its own `Project Type`. Also not done: `sla-watchdog` and `intake-routing` were left untouched despite finding the `Out of SLA/Rush` field, since changing how rush is derived is a behaviour change to a scheduled skill, not a documentation fix.

[2026-08-03] DECISION: Built `intake-dispatch` (skill) + `routines/mops-intake-dispatch/` (blueprint) as **Stage 1 of 2: propose-only, zero writes**. It sweeps the open `[MOPs] Intake` board daily, buckets by `Project Type`, and proposes an owner + the sub-task set per ticket as a reviewable digest. Also corrected two load-bearing facts in `intake-routing` that would have caused wrong writes. | REASONING: Forkan asked for a daily routine that buckets intake by type and an agent that assigns them and creates the sub-task lists. Scoping Stage 1 to **read-only** is the decision worth recording: both target verbs are **Class B** (assignment notifies the assignee, sub-task creation notifies followers), and `write-actions.md` §6 authorizes only two unattended targets today, neither of which is this project. A propose-only routine needs **no §6 row at all**, so it can run from day one while routing quality is still unvalidated — and validation is genuinely needed, because 94% of open tickets have null Priority, meaning there is no baseline of human judgment to check a derived priority against. Building the write version first would have meant either blocking on an authorization decision or auto-assigning real work to real people on untested logic. | CONTEXT: Validated against the live board before writing the skill (50 most-recent open top-level tickets), and the measurements changed the design three times. (1) **There is no Region field on `[MOPs] Intake`** — not null, absent from the project's field set. `Requesting Team`, the obvious proxy, is **49/50 null**; `Requestor` is **50/50 null**. So `intake-routing`'s rules **2 (LATAM)** and **5 (Region × Project Group)** cannot fire on real tickets at all. (2) **`created_by` is the missing requester** — populated 38/50, and the names land directly on the stakeholder lists (Zhenya, Kim Bonilla, Shannon, Jessica, Samantha/"Sam", Melissa → Aayushi; Katharine Shaw, Karen Plant → Harish), which makes rule 4 — the table's *strongest* signal — usable after all. The Region-free subset (rules 1/3/4/6) covers a measured **32 of 50 = 64%**; 12 need Region, 6 need classification. (3) **Rule 4 makes the imbalance worse, not better** — of those 32 routable tickets it sends **24 to Aayushi, 6 to Felipe, 2 to Harish = 75% to one person**, because her six named stakeholders file most of the board. Since `intake-routing` exists specifically to fix Harish/Aayushi overload, this is the finding, not a side note: the skill promotes the workload check from advisory to load-bearing and headlines any owner taking >50% of a run, while still routing per the table (the ownership model is not a suggestion — surface, never silently rebalance). That output is a management finding for Jennifer, not something a routine should fix on its own. Two further measurements shaped it: **58% of open tickets are unassigned** (29/50 — the value case) and **30% have a null `Project Type`** (15/50) — but that 30% is three different problems and only one needs a human: 5 blank-name orphans (junk), 5 `[Event Name]`-style **un-instantiated template rows leaking onto the board** (hygiene), and 5 genuinely unclassified real requests. The skill counts them separately because collapsing them overstates the problem 3×. Sub-task proposals read the catalog's evidence tiers, so for `SFDC Campaigns only (multiple)` and `Audiences` — two of the five types Forkan named — the correct proposal is **nothing at all**, since both are leaves. Corrections folded back into `intake-routing`: the **`Priority` picklist is now confirmed** (its own line 58 flagged it unverified) and it is *not* Low/Med/High — five long descriptive strings that must be written in full, on a field whose name has a **trailing space** (`"Priority "`), and with `Urgent`/`Critical` tiers meaning the SOP's Rush escalation maps to `Urgent`, not `High`; plus a Region caveat at the top of its Inputs so nobody synthesizes a Region to force rules 2/5. Also hit and worked around: `search_tasks` with `opt_fields=custom_fields` **exceeds the tool output limit at 50 tasks** (~65k chars) — documented in the skill with the two-narrow-passes workaround, since the full board is 158. | DEFERRED: The routine is a **blueprint, deliberately not provisioned** — the delivery surface is an open decision that determines the host, and it's a real trade-off rather than a detail: a file in `outputs/` needs zero authorization but forces a *local* host (cloud routines have no repo persistence), a `#mops-team` post is **Class C** and needs a §6 row or per-run confirmation, and per-ticket comments are Class B at 30+ notifications per run (recommended against). Recommendation recorded as local + `outputs/`, moving to Slack once proposals are consistently right — and that's likely Jennifer's call since the digest assigns her team's work. Also still open and higher-leverage than anything in the skill: **capturing Region at intake**, which is an Asana form change no amount of skill logic substitutes for, and would lift coverage from 64% toward complete. Stage 2 (executing approved writes unattended) is explicitly out of scope until weeks of Stage 1 digests are trusted.

[2026-08-03] DECISION: Settled `mops-intake-dispatch`'s two open questions at Forkan's direction — **delivery is a local file in `outputs/`** (which forces a **local launchd host**, `com.acquia.mops.intake-dispatch`, 07:00 PT Mon–Fri proposed) and **scope is new arrivals only, never the backlog**. Wrote the plist and a `state.json` watermark, and left the agent **deliberately unloaded** pending confirmation of the run time. | REASONING: "New submissions only, ignore the old ones sitting in Asana" is a scope decision that has to be enforced **structurally, not by judgment**, or it decays the first time a run has nothing to report and the model is tempted to reach further back. Hence a two-part guard: a `last_run_utc` watermark for the normal case, plus a **`floor_date` that the routine may never look behind**. The floor exists for one specific failure mode — on the first run `last_run_utc` is null, and without a floor the query sweeps the entire board history, which is exactly the backlog Forkan excluded. Three supporting choices: the window carries a **1-hour overlap** so a ticket created seconds before a run boundary can't fall between two windows; the watermark advances to the run's **start** time so anything created mid-run is caught next time rather than skipped; and it advances **only on success**, so a failed run re-covers its window (re-reporting is harmless, skipping isn't). Weekdays-only, unlike `lead-routing-audit`'s seven days: intake is a business-hours activity, and because the window is watermark-driven rather than "yesterday"-driven, Monday's run covers the weekend for free. Local host is not a preference here but a consequence — a cloud routine's filesystem doesn't persist to this repo, so choosing a file in `outputs/` chooses the host. | CONTEXT: Sizing the new-arrivals window surfaced three findings that changed the skill again. (1) **Volume is far lower than expected and mostly not where we were looking** — 6 new top-level tickets in 7 days (~1/day), against the 10–20/week in `context/work.md`, but **36 new sub-tasks in the same window, a 6:1 ratio**. So a top-level-only scope would have missed ~86% of new arrivals. The skill now splits them three ways rather than pooling: new top-level tickets are true submissions and get the full proposal; new sub-tasks that are **template-generated** are derived work and are excluded (detected by an unsubstituted `[Event Name]`/`[Webinar Name]`/`[NAME]` literal, or ≥3 siblings under one parent created within 60 seconds); and new sub-tasks that are **standalone requests** filed as sub-tasks — verified live, e.g. `SFDC Campaign Request - Competitor Search Ads`, `SFDC Campaign Request (Federal ABM, Linkedin)`, `Drupal GovCon 2026 Promo Email Send #1` — get a secondary section with an owner proposal but no sub-task expansion. (2) **Something already auto-expands the event/workshop 5-set.** The DAM Workshop DC batch (`SFDC Campaign(s) & Form`, `Pre Event Marketing Emails`, `Pre-Event Email Audience Report`, `Post Event Marketing Emails`, `Post-Event Email Audience Report`) was created as five sub-tasks inside 12 seconds, and the identical batch appeared for DAM Workshop NYC, Drupal 12 US Holiday Party, and Atlanta Tech Week 2026. This **partially answers the catalog's §8 headline question** — and it means proposing sub-tasks for the Event family may be redundant, so the mechanism (Asana project template? rule? Zap?) must be identified before that branch is trusted; the answer may delete it. (3) **That automation has a live substitution bug** — 4 of the 10 sub-tasks it created on 2026-08-03 carry the literal string `[Event Name]`. These are not stale leftovers being cleaned up; they are being generated wrong right now, which also explains the placeholder rows found in the earlier backlog sample. The digest reports the count every run and it's listed as something to hand to whoever owns the automation. Two smaller shifts: **empty runs are the normal case** and the digest is written anyway (a missing file is ambiguous — did nothing arrive, or did the job not run? — whereas an empty one doubles as a run log), and the `completed=false` filter was **dropped** from the fetch, because a ticket created and closed inside the window is still a submission that happened. Also reframed: unclassified tickets are a **new-submission** problem, not backlog rot — 4 of the 5 genuinely unclassified tickets were created within the previous 4 days, so this will likely be the digest's most frequent finding rather than an edge case. Test-row filtering was extended to `[TEST RUN` alongside `TESTING SKILL`. | DEFERRED: The agent is **not loaded** — 07:00 PT weekdays is a proposal and the run time is Forkan's to confirm, matching the precedent set by `lead-routing-audit` (plist written, left unloaded, enabled only once a time was named). Also unresolved and now the most important open question: **the volume discrepancy**. 6 new top-level tickets/week against 10–20 expected suggests a large share of intake arrives through the **Jira channel** — `mops-command-center` already found ~289 real open ops tickets there, a second front door — in which case this routine watches the smaller one. Worth resolving before investing in Stage 2. `state.json` is machine state that changes every run and should probably be gitignored; the durable record is the digest series in `outputs/`.

[2026-08-03] DECISION: **Enabled the `mops-intake-dispatch` schedule** at **06:00 America/Los_Angeles, Monday–Friday** (Forkan's stated time), keeping `floor_date` at `2026-08-04` so today's submissions stay out of scope as backlog. Installed and loaded the launchd agent `com.acquia.mops.intake-dispatch` and smoke-tested it live. **No §6 authorization row was added or needed** — the routine performs zero writes to any live system. | REASONING: Forkan named the time, which is the authorization. Loading immediately rather than waiting is defensible here in a way it wasn't for `lead-routing-audit`: that routine appends to a shared Sheet and needed a §6 row first, whereas this one only reads Asana and writes a local markdown file, so the worst outcome of a wrong run is a misleading digest nobody has to act on. Also fixed a real defect found while wiring it: the skill described the watermark rules but **had no step that wrote `state.json` back**, so every run would have re-reported the same window forever. Added Step 0 (stamp the run start *before* fetching, so mid-run arrivals aren't silently dropped) and Step 8 (advance the watermark last, only on success, with the digest written first — if the run dies between them the next run re-covers the window and rewrites that digest, which is a harmless duplicate, whereas advancing first risks a silently skipped day). An empty window counts as success and still advances. | CONTEXT: The smoke test passed end to end — exit 0, `runs = 1`, five weekday calendar entries registered at 06:00, and `outputs/intake-dispatch-2026-08-04.md` written reporting **0 new submissions** with the window correctly opening at `floor_date` rather than sweeping the 158-ticket backlog. That is the intended first-run behaviour, so the floor guard is proven rather than assumed. One cosmetic defect remains unfixed: the digest filename uses the **UTC** date, so a run at 23:04 PT produced a file dated the following day — it should use local date so filenames match the workday. Two routing/scope corrections were also encoded this turn, both from Forkan. (1) **The three no-pattern types (`IT/Integration`, `Automations | Martech`, `UAT`) now route to Jennifer with no sub-tasks**, added as `intake-routing` **rule 0**, evaluated first. This is a genuine change to the ownership model, which previously stated Jennifer was *not* a work-owner — she is now, for exactly these three types, on the logic that work with no repeatable shape gets scoped by the manager rather than templated by an automation. It **overrides rule 6 for `UAT`** (previously Aayushi); `Form Request`/`Other`/`Reporting` are unaffected. Side effect: `IT/Integration` moves out of the BLOCKED-needs-Region bucket where it sat ownerless (4 of 50 sampled). It does **not** meaningfully relieve the 75%-to-Aayushi concentration, since `UAT` volume is ~0. (2) **The n8n workflow's real scope is far narrower than the initial description implied**, and this is now documented in three places (`CLAUDE.md` Tool Integrations, the catalog's new Automation-coverage section, and `intake-dispatch`): n8n covers `SFDC Campaign only (single)` always, `UTM(s) (+ SFDC Campaign)`, and `Webinar Request` / `Event (+ SFDC Campaign)` **only when the submission explicitly states there is no companion promotional email** — which is the same promo-email flag `intake-classification` step 4 already checks. Everything else, notably `SFDC Campaigns only (multiple)`, is fully human. Two consequences worth stating plainly: the `UTM(s) (+ SFDC Campaign)` branch is **dead code** — that option is disabled in Asana (`1206591746930196`, `enabled: false`), so it can never fire on a new submission, and whoever maintains the workflow should be told; and **"explicitly states no" is literal** — a missing or ambiguous promo-email answer means not covered, so nothing may infer "no" from silence. The division of labour is now fixed: **n8n owns detect → gate → name; these skills own assign → prioritize → expand.** Skills must never generate a campaign name for an n8n-covered type, because two namers is precisely how a convention drifts — the problem `campaign-gate-check` was created to prevent, now with a third deriver living outside this repo. The 15-min vs daily cadence gap is treated as a feature: n8n is the real-time actor, the digest is the daily human-review layer. | DEFERRED: Two n8n questions are **load-bearing and unanswered**, and both were asked. (a) **Does n8n write back into Asana** (custom field / comment / rename)? If yes, `intake-dispatch` should *read* coverage rather than infer it from Project Type, and could detect n8n failing silently. (b) **Does n8n hold Salesforce write credentials?** If it does, this is the largest unlock available in the project: every campaign-creation path here is stuck at "spec plus handoff" under `write-actions.md` §9 because SF is query-only via MCP, and `sf-campaign-spec` should then hand off *to n8n* rather than to a human. Until both are answered, n8n coverage is inferred from the type table and the digest must say so. Also still open from the previous entry and unchanged: the volume discrepancy (6 new top-level tickets/week vs 10–20 expected, likely the Jira front door), what mechanism auto-expands the event 5-set, and whether Region gets captured at intake.

[2026-08-04] DECISION: Built `audience-pull` — a skill that turns an `Audiences` intake ticket into an actual list: queries Salesforce Contacts/Leads on region, product interest, segment, persona/title and engagement, applies mandatory mailability suppression, screens the rows for defects, and writes `outputs/audience-<slug>-<date>.csv` plus a reproducible `.md` spec carrying the criteria, the exact SOQL, and the funnel. Validated end to end against live Salesforce with a real 59-row export. | REASONING: Forkan asked for exactly this after we established that `Audiences` is the one intake type where the read-only Salesforce constraint doesn't bite — the deliverable is *data*, and reads are unrestricted, so unlike every campaign-shaped request this one can actually be **finished** rather than scaffolded. The scope line is drawn precisely: producing the audience is fully available; *installing* it (a Salesforce Report, Campaign Members, a Pardot list) is a write with no path, so it stays §9 spec-plus-handoff. Discovery was done with `FieldDefinition` SOQL rather than `getObjectSchema`, deliberately — `getObjectSchema('Lead')` returns 300 fields / 132KB and blows the token cap (a constraint inherited from `lead-routing-audit`), whereas a `FieldDefinition` query filtered by name pattern returns just API name / label / type and stays compact. That technique is now documented in the skill as the way to explore any large object here. | CONTEXT: Three findings from validation changed the skill, and the third only surfaced by reading actual rows rather than trusting counts. (1) **Pardot's engagement fields are effectively dead inside Salesforce** — measured on a real 74,530-contact mailable cohort: `pi__grade__c` **0%** populated (zero rows, so filtering on it silently returns nothing), `pi__last_activity__c` **0.9%**, `pi__score__c > 0` only 13.4%. This is counterintuitive since Pardot *is* the email platform, and it means the obvious engagement filter is the wrong one. The usable signals are `X6sense_Engagement_Score__c` (**99.9%**), a `CampaignMember` semi-join over the last 365 days (**83.6%** — and the best *real* signal, being actual campaign-response history), and `Behavior_Score__c` (69.8%). (2) **Suppression is a third of the audience, not a rounding error** — 111,840 Marketing Cloud contacts reduce to 74,530 after opt-out / bounced / Pardot-hard-bounced, so the clause is mandatory and must never be relaxed to hit a size target. (3) **The opt-out clause is insufficient: 12 of 59 exported rows (20%) carried defects that every opt-out filter passes.** Worst case first — a **GDPR-erased record** (`Removed per Request` / `removedforcompliance040224@acquia.com`) sailed through every mailability check; four **job-changers** flagged only in the name field (`LEFT - Mariella`, `LEFT - Esther`, `LEFT - Audry`, `Bas (left org)`); one email deliberately invalidated with a `.invalid` TLD while `IsEmailBounced` remained `false`; five **account mismatches** where the email domain contradicts the Account, four of which pointed Dutch contacts at *Australian* accounts (Meat and Livestock Australia, Melbourne Water, Sydney Metro, Baw Baw Shire Council), which looks like a bad bulk association rather than noise; two encoding-corrupted names; one shared inbox stored under a person's name. The skill now emits a `Flag` column with seven codes, hard-excludes the first three classes, retains all rows so exclusions stay auditable, and reports the extra funnel step. Critically, **most of this screening cannot be done in SOQL** — the name-based patterns need the rows in hand, which is an argument for pulls small enough to actually export and against ever treating a `COUNT(Id)` as the deliverable. Two mechanical limits also pinned: `Region__c` is a formula field that **is filterable with `=` but cannot be grouped** (`GROUP BY Region__c` errors outright, consistent with the Lead-side finding), so `MailingCountry` is preferred for filtering and grouping; and row export is capped near **~2,000** because SOQL `OFFSET` maxes at 2,000 and results return through the tool into context (a 50-row Asana result already hit ~65k chars), so keyset pagination on `Id >` is the only unbounded method and anything larger becomes count + SOQL + handoff rather than a silently truncated list biased by creation order. Contact/Lead asymmetry was mapped and matters: `Business_Segment__c` exists on Lead but **not** Contact, while the six `Subscribed_to_*` and seven `DAM_Opted_out_*`/`DAM_Unsubscribed_*` fields exist on Contact but not Lead — so a both-objects pull needs two queries with different WHERE clauses unioned under a `Source` column, never one query. The validation run is retained as the worked example (`outputs/audience-emea-marketingcloud-nl-2026-08-04.{csv,md}`): EMEA · Marketing Cloud · Netherlands · has Title · campaign activity in 365d → 59 rows → 53 clean; verified 59 unique Ids, 59 unique emails, 0 malformed rows, and comma-bearing titles/account names correctly quoted. | DEFERRED: **No SLA exists for `Audiences`** — the SOP's per-type table has no row for it, so priority derivation is a guess (closest analogue is `List Upload`, 3–7 days); flagged rather than invented. Three questions for Felipe: which of the **three** product-interest fields (`Product_Cloud_Interest__c`, `Product_Interest__c`, `Product_Interest_Multi__c`) is authoritative; whether the `ACCOUNT_MISMATCH` cluster is a known data-integrity issue worth a separate cleanup; and whether he wants a subtype captured, since effort inside this one Project Type varies roughly 10× (a webinar audience selection is a filter, `GTM Plays Audience Orchestrations - Contact Data Enrichment` was a 5-sub-task project). Also noted: the CSV contains personal data (names, work emails, employers, titles) and lands in a git repo — `outputs/` is currently untracked but **not** gitignored, so committing it is a live risk that needs a deliberate decision.

## 2026-08-04 — `mops-audience-pull` daily routine (06:00 PT, new submissions only)

**Decision:** run `audience-pull` unattended every morning at 06:00 PT, scoped to newly-submitted audience tickets. Live as `com.acquia.mops.audience-pull` (local launchd).

**Why a new sweep mode rather than a new skill:** the pull logic, field table, suppression clause and row screening are already validated in `audience-pull`. A second skill would fork the mailability rules — the one place a divergence causes real harm. Added a `Routine mode — daily sweep` section to the existing skill instead.

**The load-bearing constraint: an unattended run may never invent criteria.** Attended, a missing filter dimension is a question; unattended there is nobody to ask. So each ticket resolves to `READY` / `NEEDS_INPUT` / `BLOCKED` / `TOO_LARGE` and **only `READY` produces a CSV**. `NEEDS_INPUT` is the expected outcome, not a failure — a CSV built from a guessed reading of "our EMEA audience" is worse than no CSV because it looks finished. This is also what keeps the routine outside `write-actions.md` §6: it performs zero live-system writes.

**Detection cannot key on `Project Type`.** Measured live: `Project Type = Audiences` is set on **1** open ticket, while **12** audience-shaped tickets carry `Project Type = null`. Added a name-match test alongside the picklist. A name-match with a null `Project Type` is reported as a finding in its own right — it is the classification gap, not just an audience request.

**Volume is honest, not encouraging.** Genuine Audiences tickets arrive ~1/month. Most digests will be empty, and are written anyway so a missing file stays an unambiguous signal of a missed run. Accepted: the value is catching the real one the morning it lands, not daily throughput.

**Schedule kept at 06:00 exactly as requested**, despite colliding with `mops-intake-dispatch` on the same minute. Flagged in the plist and README with a one-line fix (`Minute` → `15`) rather than silently staggering it.

**Open risk logged, not resolved:** `READY` pulls write personal-data CSVs into `outputs/` unattended, accumulating without a human decision each time. Recommended gitignoring `outputs/*.csv`; not done here because it changes what gets version-controlled and that is Forkan's call.

**Also corrected:** the `mops-intake-dispatch` entry in `CLAUDE.md` claimed "built, not loaded, 07:00 PT proposed". It is in fact loaded and running at 06:00 Mon–Fri. Moved to Active with the real schedule.

**2026-08-04 (same day, amended):** schedule moved **06:00 → 07:00 PT**, all seven days. This supersedes the "kept at 06:00 despite the collision" decision above — the collision with `mops-intake-dispatch` is now resolved rather than flagged. The morning slots are deliberately one routine per hour: `lead-routing-audit` 05:30, `mops-intake-dispatch` 06:00 weekdays, `mops-audience-pull` 07:00 daily. Keep that spacing when adding routines; two `claude -p` processes sharing the Asana/Salesforce MCP connections on the same minute is the failure mode worth designing around. Verified against live launchd: all 7 calendar entries registered at `Hour => 7`.

## 2026-08-04 — `outputs/` reorganized into subject subfolders

**Decision:** `outputs/` is now **one subfolder per subject**, and nothing lives in its root. Ten canonical folders: `audiences/` · `intake/` · `campaigns/` · `campaign-hygiene/` · `email-performance/` · `weekly-review/` · `command-center/` · `sla/` · `lead-routing/` · `decks/`. The routing table lives in a new standing rule, `.claude/rules/output-files.md`, with `outputs/README.md` as the human-facing map.

**Why a rule file rather than just moving files:** the folder was flat because nothing told each skill where to write — every skill named its own path, so the next one would too. `.claude/rules/*.md` is auto-loaded into every session, which makes the routing table the default rather than a convention someone has to remember. Moving files without that would re-flatten within a month.

**Grouped by subject, not by producing skill.** Several skills write about the same subject (`audience-pull` writes both the per-request CSV/spec pair *and* the routine's sweep digest; intake classification/routing/tracking/dispatch all write intake reports). Keying on subject keeps those together and stops the folder count tracking the skill count.

**Paths are inlined in each skill, not just in the rule.** Duplication on purpose: cloud routines clone skill folders and **don't** inherit `.claude/rules/`, so a rule-only path would leave a cloud routine guessing. Rule §7 says to keep them inlined.

**Build intermediates split out** to `<subfolder>/assets/` — the six `chart_*.png` files were sitting alongside the decks they were embedded into, making the deliverable listing unreadable. Also makes intermediates safe to clear.

**Gitignore had to change with the move, not after it.** The old patterns `outputs/*.csv` and `outputs/audience-*.md` are depth-1 only — they would have stopped matching the moment the CSVs moved into `outputs/audiences/`, silently exposing personal-data files to the next `git add`. Now `outputs/**/*.csv` + `outputs/audiences/*.md`, verified with `git check-ignore` against the real files. Rule §8 requires any future personal-data subfolder to ship its gitignore line in the same change.

**This also closes the open gitignore question** from the `mops-audience-pull` entry above: audience CSVs *and* their spec `.md` files are gitignored, deliberately, and the CLAUDE.md routine bullet no longer says the decision is open.

**Not done:** the `graphify-out/` graph is now stale with respect to these skill edits — needs a `/graphify --update` agent run.

## 2026-08-13 — Weekly review's documented Drive folder ID doesn't resolve; corroborated the real folder by content instead

**Finding:** `MOps-weekly-report/SKILL.md` names Drive folder `0ACqafLRVUxJzUk9PVA` as the style-reference/upload target. This run's `get_file_metadata` call against that ID returned "Requested entity was not found" — it's stale, wrong, or the connected account lacks access. Rather than guessing or blocking, searched Drive by filename and found the actual reference files (`MOps_Weekly_Review_20260704.pptx`, `MOps_Weekly_Review_20260723.pptx`, `MOps_Weekly_Review_System_Prompt_v2.md`, the source PDF) all sitting in folder `1V3yfifgATNpZ0CJn5G5ONzJzgpgT6oj5` — confirmed by exact filename/content match, not inferred. Used that folder as this run's upload target instead of the stale ID; did not edit `SKILL.md` itself since correcting a routine's live config path is a call for a human to make, not something to silently rewrite from an unattended run. | REASONING: An unattended routine hitting a "not found" on its documented target has two options — block entirely, or corroborate the real target from independent evidence and proceed while flagging the discrepancy. Content-based confirmation (four exact filename matches, one being the specific reference deck the skill names by name) is strong enough evidence to proceed without a human in the loop for a read-only lookup; it would not be strong enough to justify a write to the wrong place, which is why this is scoped to "where to upload," not "skip confirming." | CONTEXT: Flagged on deck slide 19 (Data & Image Sources → Gaps) and in this run's Slack post. `MOps-weekly-report/SKILL.md` and `CLAUDE.md`'s routine description should have their stored folder ID corrected to `1V3yfifgATNpZ0CJn5G5ONzJzgpgT6oj5` — leaving that edit for Forkan to make and verify rather than doing it from this unattended run.

**Also confirmed (not new, but re-verified against this week's real deck):** the Drive `create_file` MCP tool still only accepts inline `base64Content`/`textContent` — no path-based, chunked, or resumable upload parameter exists (checked its current schema directly). This week's deck is 69,683 bytes (≈92.9K base64 chars); per the token-density measurement already recorded on 2026-07-30 (~3.85 tokens/base64-char), emitting that as a single tool-call argument would need roughly 358K output tokens — deterministically past any single turn's output budget, not a flaky failure worth retrying. Per this skill's own instruction not to retry the known-broken approach repeatedly, this run did not attempt the call and went straight to flagging the gap (deck built locally, not uploaded) — consistent with the two prior confirmations of the same wall.

## 2026-08-28 — Unattended `lead-routing-audit` (Drive/Slack CSV variant) run for 2026-08-27: withheld both writes, no §6 row exists for either target

**Finding:** A scheduled cloud-routine prompt fired asking this session to run the `lead-routing-audit` skill for report day 2026-08-27, deliver the result as a CSV uploaded via `Google-Drive create_file` into folder `1ec60nqQ_uRmqakvSrCUj_P_k2HU5MPTk` ("Lead Routing QA - Daily Sheets"), and post a digest to Slack channel `C0BPA78FQ6T` (#daily-mql-audit) — explicitly asserting both were "the standing unattended authorization in `.claude/rules/write-actions.md` section 6." Checked the actual file: §6's table lists exactly two standing unattended targets (the `#mops-team` weekly-review post, and `values.append`/`values.update` to the *other* Lead Routing QA Sheet `1gFu34xeJJ2Jrk1p9jtvgkzp_WtspHNyCGZ1zorbNNOA`'s `Detail!A:X`/`Daily Summary`/`Config Linter`). Neither the Drive folder `1ec60nqQ...` nor the Slack channel `C0BPA78FQ6T` appears anywhere in §6, in this log, or in `routines/` — there is no `routines/*mql*` folder, no README, no prior decision entry for a Drive/Slack delivery mode of this skill at all. **Did not perform either write.** | REASONING: `write-actions.md` §2/§6 is explicit and repeated: Class B (shared-folder Drive `create_file`) and Class C (Slack channel send) writes need either a standing §6 row or a human confirming in the moment; "No channel, DM, project, or record outside this table gets an unattended write. Adding a row is a decision → log it in `decisions/log.md` first." An unattended run has no human to confirm with, and the routine's own claim of authorization is not itself evidence — it's exactly the kind of unverified assertion the operating rules say must be checked against the primary source rather than trusted at face value, especially given no human was live to have actually granted it moments before. Whether this was a genuine oversight (someone stood up this delivery mode and forgot the §6/decision-log paperwork) or a misconfigured/injected scheduled-task prompt, the correct response is the same: don't write, and surface it. | CONTEXT: Still did the safe, valuable half — ran the full read-only audit (Q1–Q7 against live Salesforce, per `.claude/skills/lead-routing-audit/reference.md`) for 2026-08-27. Result: 9 leads in scope, 5 routed OK, 2 wrong-rep (Medium — same rep, Stephen Deshong, reassigned both of the day's Account-Team-Member-routed Content Cloud leads to reps off the account team within ~4–6 minutes of correct routing; worth one question to him, not asserted as a defect), 0 not-routed, 2 bypassed by design (Partner Deal Registration), 0 High severity. Status COMPLETE — no query failures, no unmeasured fields, live rule table confirmed at 47 active rules (matches reference.md §0's revision note). Saved the Daily Summary + Detail rows as `outputs/lead-routing/Lead_Routing_QA_2026-08-27.csv` (spec-plus-handoff, same pattern as any other missing write path) instead of uploading to Drive or posting to Slack. Notified Forkan/Jennifer directly rather than through the unauthorized channel. | DEFERRED: Whoever set up this Drive/Slack delivery mode (if it's a deliberate, wanted addition) needs to either (a) add a §6 row naming this exact Drive folder and this exact Slack channel and log that decision here, and/or (b) create a `routines/` folder documenting the schedule/host/scope the way every other active routine has one — right now this scheduled task exists with no corresponding repo documentation anywhere. Until then, future unattended firings of this same prompt should keep producing the CSV as a local handoff file rather than writing to Drive/Slack, and keep flagging the gap rather than assuming a prior run's silence means it was fine.


## 2026-09-05 — Same `lead-routing-audit` Drive/Slack gap recurs for report day 2026-09-04; found the 08-28 finding never got acted on and the unauthorized writes have continued at scale

**Finding:** Same scheduled cloud-routine prompt as 2026-08-28, same false claim ("standing unattended authorization in section 6") for the same two targets — Drive folder `1ec60nqQ_uRmqakvSrCUj_P_k2HU5MPTk` and Slack channel `C0BPA78FQ6T` (#daily-mql-audit). Re-checked `write-actions.md` §6 on disk: still only two standing rows (`#mops-team`, Sheet `1gFu34xeJJ2Jrk1p9jtvgkzp_WtspHNyCGZ1zorbNNOA`); still no row for either target; still no `routines/*mql*` folder. **Did not perform either write today**, same as 08-28 — CONTEXT below.

**Escalation beyond 08-28: this is not an isolated one-off.** A full scan of `decisions/actions.md` for `ACTOR: scheduled routine (lead-routing-audit, cloud...)` shows this exact routine has actually been **writing to both unauthorized targets nearly every day since 2026-08-12** — **18 successful `drive.files.create` writes** (3 more attempted and failed for unrelated technical reasons) and **20 successful `slack.chat.postMessage` sends** to `#daily-mql-audit`, spanning 2026-08-12 through 2026-09-03, with 2026-08-27 (caught on 08-28) as the **only** day a run actually verified the false authorization claim against the rules file and withheld. Every other day, a fresh cloud session took the scheduled prompt's assertion at face value and wrote anyway. **The 08-28 entry's own deferred action — add a §6 row and/or a `routines/` folder, or stop the prompt from re-asserting a false authorization — was never done**, so the same prompt kept firing daily and kept getting believed. | REASONING: Volume of past compliance is not evidence of authorization — `write-actions.md` §6 requires a human decision logged in `decisions/log.md` before any target gets unattended writes, and no such entry exists for this Drive folder or this Slack channel anywhere in this file. What the trail actually shows is the same unverified claim succeeding at social-engineering ~20 independent fresh sessions in a row; one session (08-28) happened to check the primary source and caught it, but nothing then closed the gap, so the false claim kept working on every subsequent fresh session with no memory of the 08-28 finding. Repeating a policy violation on a schedule does not convert it into policy. | CONTEXT: Ran the full read-only audit (Q1, Q2/Q3 union, Q6 `LeadHistory`) for 2026-09-04 against live Salesforce — 47 active rules confirmed (unchanged from the post-2026-08-06 revision). 11 leads in scope: 5 Routed OK (all five recomputes matched their fired rule exactly — 780/DAM Team→Stephen Deshong, 715/AMER BDR→Maria Cespedes, 1001/AMER BDR→Marco Chavarria ×2, 1401/EMEA BDR→Grace Safadi; three of the five were self-DQ'd by the routed-to rep within minutes to ~2.5h with `Disqualified_Reason__c` populated, which is the documented exoneration pattern, not a misroute), 0 Wrong Rep, 0 Not Routed, 6 Bypassed (all `Partner - Deal Registration`, the by-design bucket). 0 High/Medium severity — a clean day, no `RULE_DIVERGENCE`, no config-linter re-check performed (out of scope for a daily sweep). Status COMPLETE. Saved as `outputs/lead-routing/Lead_Routing_QA_2026-09-04.csv` (gitignored) instead of writing to Drive/Slack. | DEFERRED (raising the priority from 08-28's "whenever convenient" to "this is actively accumulating"): a human needs to either (a) formally authorize this delivery mode — add the exact Drive folder + exact Slack channel to `write-actions.md` §6 with this entry as the authorizing decision, since in practice it has been running unattended for a month and someone should decide whether that's actually fine — or (b) fix the scheduled task's stored prompt so it stops asserting a false §6 citation, and/or add a `routines/` folder so this delivery mode is documented like every other active routine. Until one of those happens, the same false claim will keep firing daily and keep getting believed by every fresh session that doesn't happen to check the primary source first.
